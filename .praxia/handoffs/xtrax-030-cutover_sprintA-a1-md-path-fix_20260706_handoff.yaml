---
workspace: xtrax-030-cutover
session_id: 64f064d0-f73b-4f23-b835-360683c646bf
status: in_progress
phase: Sprint A / A2
task_id: 260706_xtrax_pathc
goal_statement: >
  Sprint A (.praxia/docs/specs/260706_b1-core-md-path-fix.md): fix prolix's
  MolecularBundle MD path so it can run a real vacuum protein, unblocking the
  B1 Claim-1 benchmark. Three work items: A1 add a `masses` field to
  MolecularBundle (DONE, resolved a regression along the way), A2 JIT-safe
  nonbonded exclusions (not started), A3 settle_langevin vacuum-protein dt
  investigation (not started).

summary: >
  A1 is complete and verified clean (commits a048f61 WIP + e41854b fix, both
  pushed to worktree-xtrax-030-cutover / PR #1). The full-suite regression
  flagged in the WIP commit was root-caused precisely: masses_for_bundle()
  had a JIT-illegal data-value branch (`bool(jnp.all(m == 1.0))`), fixed by
  making it a pure slice of bundle.masses. Verified against the pre-A1
  baseline (a4f455f) that the only remaining failures (4, in
  tests/api/test_v3/v4/v5 + test_multi_bundle_run) are byte-identical
  pre-existing failures (ConcretizationTypeError, stacked-dispatch bug,
  unrelated to A1/A2/A3 — see deferred). Zero new failures from the A1 fix.
  Next: A2 (JIT-safe exclusions).

immediately_relevant:
  - file: src/prolix/api/bundle_md.py
    lines: [95, 106]
    reason: >
      masses_for_bundle — now a pure slice (`return bundle.masses[:n_atoms]`),
      no runtime branch. This is the A1 fix. Also see
      _dense_excl_matrices_from_bundle (~line 175) and
      physics_system_from_bundle (~line 203) — the A2 target. The module
      docstring (lines 1-18) documents the JIT contract explicitly: never
      branch on bundle field *values* inside a function reachable from
      energy_fn_from_bundle's jit trace; int()-ing scalar count fields
      (n_atoms, n_excl, etc.) is the existing (if fragile) convention for
      host-only code paths.
  - file: src/prolix/physics/system.py
    lines: [632, 651]
    reason: >
      make_bundle_from_system — masses_in handling, unit-mass default
      preserved for callers without real data. Unchanged since A1's first
      pass; still correct.
  - file: src/prolix/types/bundles.py
    lines: [121, 130]
    reason: >
      MolecularBundle field list — masses field, 2nd position after
      positions. Turned out NOT to be the cause of the regression (position
      was a red herring; the actual bug was the runtime bool() branch). No
      further action needed here.
  - file: .praxia/docs/specs/260706_b1-core-md-path-fix.md
    lines: []
    reason: The full Sprint A plan, verified diagnosis, and acceptance criteria.
  - file: scripts/benchmarks/_b1_paramize.py
    lines: [1, 20]
    reason: >
      A2's target — the keystone PDB-to-bundle loader. Still does NOT pass
      exclusion_spec= to make_bundle_from_system; has an honest limitation
      docstring already added.
  - file: src/prolix/simulate.py
    lines: [485, 495]
    reason: >
      ExclusionSpec.from_protein(protein) — the existing, working exclusion
      builder A2 should reuse (don't reinvent).
  - file: src/prolix/padding.py
    lines: [160, 175]
    reason: map_exclusions_to_dense_padded — the existing JIT-safe padding helper A2 should reuse.

active_dispatches: []

context_refs:
  - phase: B1 harness build
    task_id: 260706_xtrax_pathc
    summary: >
      b1_init_exec.py + _b1_paramize.py keystone loader built and pushed
      (f2d05ff/1ca3978/ccbc13c); L1/L2 verified for init-bound mechanics
      only — protein MD is what Sprint A unblocks.
  - phase: Path B delegation
    task_id: 260706_xtrax_pathc
    summary: xtrax 0.3.0 cutover (1414fb6) + BatchPlanner delegation (a519a06) — clean, unaffected.
  - phase: Sprint A spec + diagnosis
    task_id: 260706_xtrax_pathc
    summary: >
      Root-caused B1 protein-MD divergence to dropped exclusions (not
      minimization) + no mass field + settle dt. Spec at a4f455f.
  - phase: Sprint A / A1 (superseded)
    task_id: 260706_xtrax_pathc
    summary: >
      Prior handoff version of this file recorded A1 as blocked on an
      undiagnosed regression. Now resolved — see summary above. That
      version's failed_attempts entries are preserved below for the
      historical record.

failed_attempts:
  - attempt_id: a1_masses_field_regression
    description: >
      Added `masses` field to MolecularBundle, wired through
      make_bundle_from_system, updated masses_for_bundle to prefer
      bundle.masses with a TIP3P-water fallback for legacy all-ones callers,
      updated all 9 direct MolecularBundle(...) test constructors.
    outcome: >
      RESOLVED (commit e41854b). The TIP3P-fallback branch
      (`if bool(jnp.all(m == 1.0)):`) was a JIT-illegal data-value branch —
      TracerBoolConversionError/ConcretizationTypeError in 22 tests, all at
      the same bundle_md.py call site, inside energy_fn_from_bundle's jit
      trace.
    reason_failed: >
      energy_fn_from_bundle's closure is traced far more broadly than its
      "host-side" docstring framing suggested. Fix: drop the runtime branch
      entirely — masses_for_bundle is now a pure slice. Safe because
      make_bundle_from_system already always populates masses correctly, so
      the fallback heuristic was unnecessary (confirmed no test fixture
      relies on it — all have n_waters=0).
    do_not_retry: true
    evidence_ref: >
      Fixed and verified: tests/api+bench+tiling+batching -m "not slow" →
      108 passed, 4 failed (all pre-existing, confirmed identical at
      baseline a4f455f), 3 skipped, 1 deselected. All 6 tests/physics files
      touching MolecularBundle/EnsemblePlan/masses_for_bundle: 23/23 passed.

  - attempt_id: a2_exclusion_jit_fix_reverted
    description: >
      Earlier attempt (before A1) at rewriting _dense_excl_matrices_from_bundle
      as a jnp scatter, alongside wiring exclusion_spec through
      _b1_paramize.py.
    outcome: >
      Fixed the immediate TracerArrayConversionError but the change was made
      without full-suite regression testing; reverted.
    reason_failed: >
      Not actually wrong in approach — ExclusionSpec.from_protein +
      map_exclusions_to_dense_padded (both already exist in prolix) remains
      the recommended A2 path. The lesson from THIS session's A1 experience
      applies directly: verify against the FULL suite after each incremental
      change, not just the tests that motivated the change.
    do_not_retry: false
    evidence_ref: Reverted via git checkout; approach documented in the Sprint A spec.

open_questions:
  - >
    A2: does wiring ExclusionSpec.from_protein through _b1_paramize.py and
    fixing _dense_excl_matrices_from_bundle's JIT-unsafety (same class of bug
    as A1 — check for int()/bool() on traced values) also fix the pre-existing
    V4/V5/V3/multi-bundle ConcretizationTypeError, since they're in adjacent
    code (physics_system_from_bundle)? Worth checking as a side effect, not a
    requirement.
  - >
    Is the pre-existing V4/V5-family stacked-dispatch ConcretizationTypeError
    in scope for Sprint A, or genuinely separate (B1's heterogeneous ensemble
    never exercises the homogeneous stacked-compile-once path)? Leaning
    "separate, deferred" — not yet asked explicitly of the user.
  - >
    What is the second slow/hanging test in tests/physics -m "not slow"
    (beyond the already-known LFMiddle one)? Traceback was deep in JAX
    vmap/pjit internals, not obviously related to any file touched this
    session. Not yet identified by name — the full tests/physics directory
    run needs a proper triage pass (e.g. run file-by-file or add
    --durations=20) rather than one big timeout-prone invocation.

deferred:
  - description: >
      Pre-existing V4-HLO/V5/V3/multi-bundle stacked-dispatch
      ConcretizationTypeError family (bisected to base commit 6b9e588 AND
      reconfirmed identical at a4f455f — predates all of this session's work).
    rationale: >
      Not required for B1 unblock since B1's heterogeneous ensemble never
      hits the homogeneous stacked-compile-once path that triggers it.
    recommended_phase: separate sprint, flagged to user but not yet actioned
  - description: Second unidentified slow/hanging test in tests/physics (not LFMiddle).
    rationale: Orthogonal to Sprint A; needs its own triage pass.
    recommended_phase: separate, low priority
  - description: Bucket/Carry/Dedup adoption in xtrax_adapter (optional Claim-1 speedup).
    rationale: Deferred since before Sprint A; unaffected by current work.
    recommended_phase: after Sprint A + B1-full

next_steps:
  - >
    A2: wire ExclusionSpec.from_protein (src/prolix/simulate.py:489) +
    map_exclusions_to_dense_padded (src/prolix/padding.py:166) through
    _b1_paramize.py's exclusion_spec= param on make_bundle_from_system.
  - >
    A2: fix _dense_excl_matrices_from_bundle's JIT-unsafe numpy loop
    (bundle_md.py ~line 175) — check carefully for int()/bool()-on-traced-value
    patterns, the SAME class of bug A1 just hit. Verify against the FULL
    suite (tests/api+bench+tiling+batching+the 6 physics files, at minimum)
    after this change, before considering it done — do not repeat the
    prior attempt's mistake of only checking the tests that motivated it.
  - >
    A2 acceptance: run the keystone self-test on 2GB1 — median |grad| should
    drop from 3.84e5 to O(10^2) kcal/mol/Å once exclusions are populated.
  - >
    A3 (unstarted): root-cause why vacuum proteins need dt<=0.01fs vs the
    pinned 0.5fs, after A1+A2 land. Compare settle_langevin vs plain BAOAB
    (no SETTLE) on a protein-only bundle.
  - >
    B1-full unblock acceptance (per the Sprint A spec): a protein bundle from
    _b1_paramize runs a finite trajectory at dt=0.5fs through
    EnsemblePlan.from_bundles(...).run(), median |grad| O(10^2), real masses.

lessons_refs: []
---
