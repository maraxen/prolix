# Loop priorities — Prolix (post xtrax-rewire audit)

# Bootstrap: XA-CLOSEOUT / AC6 (2026-07-10). Agents must honor [invariants].

[invariants]
default_ci = 'uv run pytest -m "not (slow or integration or dynamics)"'
no_autonomous_push_or_merge_to_main = true
# XA-DRIFT freeze (VACUUM-DT + exception_*):
ensemble_plan_dt_unit = "fs" # AKMA only via dt_unit="akma"
ensemble_plan_gamma_unit = "ps^-1"
vacuum_dt_policy = "gamma>=50 at dt=0.5 fs (or smaller dt @ gamma=10)"
exception_14_on_bundle_energy_path = true

[epics]
closed = "260709_xtrax_rewire" # XR-EPIC
audit = "260710_epic-audit_xtrax-rewire" # XA-EPIC
next = "260528_b1-full" # Claim 1 — locked by TRIAGE 2026-07-10; paper/HP4 deferred
