---
session_id: 33ef1b3b
topic: Decide the fix approach for prolix backlog #934 (P5-C): batched SETTLE vmap divergence — test_settle_batched_vs_unbatched passes locally but shows 8.975 Å RMSD on the Engaging cluster. Choose primary root-cause hypothesis, fix scope within settle.py, and the verification campaign that distinguishes "sufficient" from "necessary-but-insufficient".
task_type: diagnostic
winner: STAGED-BISECT (amended): Stage 0 free discriminators, then a SINGLE pipelined instrumented A/B cluster job carrying the settle_positions:171 determinism fix as hardening — with an asymmetric red/green evidence rule.

FRAMING (load-bearing, do not soften): the one-line fix is landed as obviously-correct hardening, NOT as the confirmed cause. Assumption-reversal analysis argues site 171 is probably COLD: _settle_water_batch:319 rounds a full integrator-step displacement (delta/box ~= +/-0.5 at every genuine PBC face crossing — sitting exactly on the banker's-rounding tie point), whereas settle_positions:171 rounds a sub-Angstrom SETTLE geometric correction (delta_o/box ~= 0.01 for a 10 A box, far from the tie point). The two sites are NOT symmetric hazards, so the "incomplete fix" story is plausible but unproven. The primary deliverable of this work item is therefore the MEASUREMENT (which site fires first; is the primitive error box-quantised), not the patch. A fixer must not declare victory on a green run the fix did not cause.

STAGE 0 — zero compute, all three are blocking prerequisites:
(0a) ARITHMETIC DISCRIMINATOR: read the box vector used by tests/physics/test_settle_batched_vs_unbatched and test_settle_64water_batched_10ps.py. Compare 8.975 A against it. box ~= 17.95 -> half-box -> minimum-image SIGN disagreement (H1 wrap family). box ~= 8.975 -> clean one-box jump (H1 wrap family). box >= 30 -> 8.975 is arbitrary, wrap story is WEAK, re-weight toward H3/eigh before spending the cluster job.
(0b) H2 CLOSE-BY-INSPECTION: read gamma and RNG key-threading config of both failing tests. If gamma=0, or per-replica keys are pre-split OUTSIDE the jax.lax.scan, H2-RNG is structurally excluded at zero cost — record and close. If gamma>0 with in-scan key threading, H2 stays live and gets a gamma=0 control arm added to the Stage 1 job (divergence vanishes -> H2 implicated; persists -> H2 excluded).
(0c) SCOPE GREP: `rg 'jnp\.round\(|jnp\.mod\(' src/prolix/physics/settle.py` and inspect every hit for a box-normalised argument. Already established by signature inspection: settle_velocities (line 850) takes NO box parameter and is structurally exempt. Enumerate any additional sites in the work order; convert only :171 in this item, route the rest to the deferred hardening item.

THE FIX (hardening, landed regardless of outcome):
  File: src/prolix/physics/settle.py, line 171, inside settle_positions
  OLD: image_correction = -box * jnp.round(delta_o / box)
  NEW: image_correction = -box * jnp.floor(delta_o / box + 0.5)
Rationale to put in the commit message: jnp.round is round-half-to-EVEN (banker's); under XLA FMA contraction a value at ULP proximity to +/-0.5 can round in opposite directions on the loop vs vmap path. floor(x+0.5) is deterministic round-half-up under any FMA reassociation order. This mirrors the idiom already used at _settle_water_batch:319 and makes the file internally consistent. Also update the stale comment block at lines 145-165, whose line 161 currently ARGUES FOR round() ("The only exception is a genuine PBC crossing (|delta| ~ box), handled by round()") — that sentence names precisely the regime where banker's rounding is unsafe and must not survive the change.

STAGE 1 — ONE cluster job, within-job A/B (pipelining Stages 1+2 to spend one queue round-trip, not three, on a Claim-1 critical-path item):
Extend scripts/experiments/p5_settle_batch_divergence.py (do NOT write a new diagnostic) to parameterise the rounding idiom so pre-fix and post-fix code paths run in the SAME process, SAME seed, SAME box. This makes it a true A/B where the only varying quantity is the intended one — eliminating the ambiguity where a green post-fix run merely reflects a different machine or seed.
Metric change (critical): report FIRST-DIVERGENCE STEP and the per-molecule delta at that step, NOT terminal-trajectory RMSD. A single wrap disagreement produces a +/-box error, hence a huge spurious force, hence a blown-up replica — so terminal RMSD measures the amplified cascade, not the primitive error, and is near-useless for root-causing. At first divergence, dump: the offending molecule's pre/post positions, delta_o/box, and whether |delta| is near an integer-or-half-integer multiple of box.
Run under `bth run` with a per-script sidecar (`p5_settle_batch_divergence.bth.toml`, [experiment] schema, exactly one outcome with is_residual=true) inside a slurm wrapper sourcing scripts/slurm/_bth_env.sh; register the campaign via `bth campaign create` BEFORE the first run and pass --campaign to every run.

EVIDENCE RULE (asymmetric, declared in advance, encoded in the sidecar outcomes):
  RED is conclusive on a single observation — a tie-point bug that fires proves the hazard is reachable.
  GREEN is NOT conclusive on a single observation — an FMA tie-point bug is a heisenbug by construction, firing only when a value lands close enough to +/-0.5, which is seed- and trajectory-dependent. Pass criterion is N>=5 seeds all under tolerance. Cheap, because the seeds run within the same job.

BRANCH ACTIONS (pre-declared so no result can be read post-hoc):
  (a) Post-fix arm green across >=5 seeds AND pre-fix arm red -> fix is sufficient and CAUSAL. Land it, add a cluster-marked regression test, close #934.
  (b) Both arms red -> fix is necessary-but-insufficient (the expected case under the cold-site analysis). Land the hardening anyway, and read the first-divergence instrumentation: box-quantised primitive error -> a wrap site we have not found (feed the Stage 0c grep list); arbitrary-magnitude error -> H3/eigh. Split #934, route the residual to a targeted follow-up.
  (c) BOTH arms green across >=5 seeds -> the bug was already resolved by the _settle_water_batch fix and the 8.975 A docstring records the historical PRE-fix state. Close #934 as already-resolved, land the hardening, and add the cluster-marked regression test so it cannot silently regress.

CLUSTER RE-VALIDATION IS MANDATORY. The bug is DEFINED as "passes locally, fails on Engaging" — a local green run is not evidence of anything and must not be accepted as closure. Submit via myxcel/sbatch per project rules (verify partition/walltime flags are actually inherited; --time/--partition non-inheritance bug observed 2026-08-11). Do not run the 20k-step suite on the local WSL2 box.
Regression guard: confirm the dt=1.0 fs cap-lift gate is not disturbed (reference job 19774893) before merging.
created_at: 2026-08-12T14:13:30.610404+00:00
---

# Brainstorm: Decide the fix approach for prolix backlog #934 (P5-C): batched SETTLE vmap divergence — test_settle_batched_vs_unbatched passes locally but shows 8.975 Å RMSD on the Engaging cluster. Choose primary root-cause hypothesis, fix scope within settle.py, and the verification campaign that distinguishes "sufficient" from "necessary-but-insufficient".

## Problem Frame
FIXED (cannot change):

- The physics contract: SETTLE must preserve rigid TIP3P geometry and the molecule's COM, and must not damp genuine molecular rotation (the Horn quaternion rigid fit exists specifically to avoid rotation damping — regressing that reopens the P5 C3 angular-momentum work, 678c9cb).
- The public API surface: settle_positions is called by all 7 production integrator sites (settle_langevin, settle_lfmiddle_langevin x2, settle_with_nhc, settle_csvr, settle_csvr_npt x2). Its signature cannot change.
- Bit-for-bit loop/vmap equivalence is the requirement under test. This is not "close enough" — the whole Claim-1 thesis is that one compiled kernel serves heterogeneous bundles, so a vmap replica must reproduce the looped result.
- The bug's definition includes its environment: manifests at cluster/box scale on Engaging, not locally. Any evidence standard must respect that.
- Project rules: bth run + per-script sidecar for anything in scripts/experiments/; cluster submission via myxcel/sbatch; no whole-suite pytest runs locally (WSL2 box, 29Gi RAM).
- Cannot regress the existing dt=1.0 fs cap-lift validation (gate job 19774893).

NEGOTIABLE:

- Which rounding idiom is used, as long as it is FMA-reassociation-deterministic. floor(x+0.5) is the incumbent because _settle_water_batch already uses it and internal consistency has independent value, but a different deterministic formulation is acceptable if argued.
- Whether the second re-wrap in settle_positions is FIXED (change round -> floor) or ELIMINATED (argue it is redundant given _settle_water_batch already unwrapped, and the constrained output is within a bond length of the unconstrained input). Both are on the table; elimination is a bigger behavioural change but removes the hazard class rather than patching it.
- Verification depth and ordering: which tests, on what hardware, in what sequence, and whether the diagnostic script runs BEFORE the fix (to positively confirm the mechanism) or AFTER (to characterise any residual). This is a real design choice, not a detail.
- Whether hypotheses (2) and (3) get ruled out empirically in this work item or get split into a follow-up backlog item gated on the branch-(b) outcome.
- Scope breadth: minimal single-line change vs a sweep of every PBC-rounding site in settle.py.

ASSUMPTION I want stress-tested, not accepted: that the 8.975 Å figure in the _settle_water_batch docstring refers to the CURRENT (post-fix-in-that-function) state of the test rather than the historical pre-fix state. If that number was recorded BEFORE the floor() fix landed in _settle_water_batch and nobody re-ran the cluster test afterwards, then we do not actually know the bug still reproduces — and the first action is a reproduction run, not a patch. The framing "incomplete fix" presumes the test is still red; that presumption is unverified.

## Idea Pool
- [user] H1-PATCH: Primary root cause is the unfixed banker's-rounding site at settle_positions:171 (`image_correction = -box * jnp.round(delta_o / box)`) — an incomplete propagation of the fix already landed at _settle_water_batch:319. Fix = mirror the incumbent idiom: `image_correction = -box * jnp.floor(delta_o / box + 0.5)`. Requires assumption: delta_o/box actually reaches ULP proximity to ±0.5 in practice, which by the code's own comment happens exactly at a genuine PBC crossing.
- [user] H1-ELIMINATE: Same root cause, different remedy — delete the second re-wrap in settle_positions (lines 166-175) entirely rather than making it deterministic. Argument: _settle_water_batch already unwrapped to a consistent image and returns positions within a bond-length of pos_oxygen_new, so a second image decision is redundant work that only exists to re-impose a wrap the caller's shift_fn will apply anyway. Removes the hazard class instead of patching one instance. Requires assumption: no downstream consumer depends on settle_positions returning strictly in-box coordinates.
- [user] H2-RNG: Root cause is RNG key-threading divergence under padding — padded bundle rows advance jax.lax.scan a different number of times than unpadded rows, so noise draws desynchronise when gamma>0. Currently untested as a cluster-divergence cause because the reference test test_settle_padding_mask_matches_unpadded pins gamma=0, deliberately sidestepping it. Assumption it requires: the failing cluster test runs with gamma>0. If the failing test is also gamma=0, H2 is structurally excluded and can be closed by inspection, not experiment.
- [user] H3-EIGH: Root cause is XLA precision in the Horn quaternion rigid fit (jnp.linalg.eigh, ~line 383) — eigenvector sign/ordering for near-degenerate eigenvalues is not guaranteed stable across loop vs vmap lowering, so a replica can pick an opposite-sign quaternion and produce a different (but equally valid-norm) rotation. Distinguishing feature vs H1: an eigh sign flip produces a rotation-magnitude error, NOT an integer multiple of the box. 8.975 Å against a box edge is the discriminator — if the observed RMSD is a clean fraction/multiple of box, it is a wrap bug; if it is an arbitrary sub-box number, it points at eigh.
- [user] Four ideas recorded. Now the assumption reversals, which is where the useful structure appeared:
- [user] REVERSAL 1 — "assume the bug still reproduces" -> reverse it: assume it does NOT. The 8.975 Å number lives in a docstring describing why _settle_water_batch was fixed. Docstrings record the motivating failure, i.e. the PRE-fix state. Nobody has shown a post-fix cluster run. So the honest state is: unknown whether red. This reverses the whole plan ordering — reproduction must come first, and it is cheap because the diagnostic script already exists.
- [user] REVERSAL 2 — "assume the two rounding sites are equally hazardous" -> reverse: they are NOT symmetric. _settle_water_batch:319 rounds `delta = pos_new - pos_old`, a full integrator-step displacement, which at a genuine PBC crossing is ≈ box, so delta/box ≈ ±0.5 — sitting exactly on the tie point every time a molecule crosses a face. That is a high-frequency hazard. settle_positions:171 rounds `delta_o = pos_oxygen_c - pos_oxygen_new`, a SETTLE geometric correction, which is sub-Ångström in normal operation — delta_o/box ≈ 0.01 for a 10 Å box, nowhere near 0.5. So the "identical hazard" framing overstates the case: site 171 only reaches the tie point if the constrained position has already been displaced by ~half a box, which means something upstream already went wrong. This DOWNGRADES H1-PATCH from "certainly the bug" to "a real latent hazard that is worth fixing regardless, but may not be the active cause." That is a materially different claim and it changes what the verification has to prove.
- [user] REVERSAL 3 — "assume 8.975 Å is a wrap signature" -> reverse: check it. If box ≈ 18-20 Å then 8.975 ≈ half-box, which is the signature of a minimum-image sign disagreement. If box ≈ 9 Å it is a clean one-box jump. If box is 30+ Å then 8.975 is an arbitrary number and the wrap story is weak. This single arithmetic check is nearly free and discriminates H1 from H3 before any compute is spent.
- [user] REVERSAL 4 — "assume one cause" -> reverse: assume the divergence is a CASCADE. A single wrap disagreement on one molecule at step k produces a ±box positional error, which produces a huge spurious force, which blows that replica's trajectory apart over the remaining steps. Under that model the 8.975 Å RMSD is not the size of the primitive error but the amplified downstream consequence, and any RMSD-at-end-of-trajectory metric is nearly useless for root-causing. The diagnostic must therefore measure FIRST-DIVERGENCE STEP and per-molecule delta at that step, not terminal RMSD.
- [user] Two further candidate approaches follow from those reversals.
- [user] REPRO-FIRST: Ordering approach — before touching any code, re-run the two failing tests on Engaging at current HEAD to establish whether the bug still reproduces post-_settle_water_batch-fix. Cost is one cluster job. Decides between "we have a live red test" (proceed to patch) and "the docstring recorded a historical failure already fixed" (in which case #934 closes as already-resolved plus a regression test, and the settle_positions:171 site becomes a separate latent-hazard hardening item). Cheapest possible discriminator and it gates everything downstream.
- [user] FIRST-DIVERGENCE-INSTRUMENTATION: Change the diagnostic metric from terminal-trajectory RMSD to first-divergence-step detection — step through loop and vmap paths in lockstep, record the earliest step at which any per-atom delta exceeds a tight tolerance, and dump that molecule's pre/post positions, delta_o/box ratio, and whether |delta| is near an integer-plus-half multiple of box. This directly reads out WHICH rounding site fired and whether the primitive error is box-quantised (H1) or arbitrary-magnitude (H3), instead of measuring the amplified cascade. Extends scripts/experiments/p5_settle_batch_divergence.py rather than replacing it.
- [user] DETERMINISM-SWEEP: Broaden scope from one line to a hazard-class sweep — grep settle.py (and ideally the whole physics package) for every `jnp.round(` applied to a box-normalised expression, plus every `jnp.mod(..., box)`, and convert all of them to the deterministic floor(x+0.5) idiom in one pass. Rationale: the bug's actual lesson is that the codebase has two competing rounding idioms and the unsafe one keeps surviving in places nobody re-audited. Fixing the class is barely more expensive than fixing the instance, and it makes the invariant greppable/enforceable. Risk: touches code paths outside the failing test's blast radius with no test coverage proving those sites were safe to change.
- [user] TOLERANCE-DOC: Backlog's alternative exit — accept the divergence as irreducible FMA nondeterminism, document a numerical tolerance with written justification, and relax the test assertion to that bound. Only defensible if the divergence is shown to be bounded, small, and non-amplifying. Directly contradicted by the observed magnitude (8.975 Å is not a tolerance, it is a broken trajectory) and by the Claim-1 thesis requiring loop/vmap equivalence. Keep on the record as the branch-(b) fallback ONLY if instrumentation shows a residual that is genuinely sub-Ångström and non-cascading.
- [user] H2-CLOSE-BY-INSPECTION: Rather than spending a cluster job isolating the RNG-padding hypothesis, close or confirm it by reading the failing test's configuration. If test_settle_batched_vs_unbatched and test_settle_64water_batched_10ps run with gamma=0 or with per-replica keys pre-split outside the scan, H2 cannot be the cause and is excluded at zero compute cost. If they run gamma>0 with in-scan key threading, H2 stays live and needs its own controlled run (same test, gamma forced to 0: if divergence vanishes, H2 is implicated; if it persists, H2 is excluded). This converts a vague "we should also rule out RNG" into a concrete two-branch, near-free test.
- [user] STAGED-BISECT (composite): Sequence the cheap discriminators before the expensive ones, with pre-declared branch actions. Stage 0 (free, no compute): arithmetic check of 8.975 Å against the failing test's box vector; read gamma/key-threading config of both tests to close-or-keep H2; full-file grep for jnp.round-on-box to fix the scope question. Stage 1 (one cluster job): reproduce at current HEAD to establish red/green. Stage 2: apply the settle_positions:171 floor fix + instrumented first-divergence diagnostic, re-run on cluster. Branch (a) green -> land fix, add cluster-marked regression test, close #934. Branch (b) still red -> instrumentation now tells us whether the primitive error is box-quantised (a wrap site we missed) or arbitrary (H3/eigh), and #934 splits with the residual routed to a targeted follow-up. Every stage has a declared decision rule before it runs, so no stage can be interpreted post-hoc.
- [user] converge

## Decision Log
- [REJECT] TOLERANCE-DOC (accept divergence as irreducible FMA noise, document a tolerance, relax the assertion): Explicitly considered per the backlog's stated alternative exit condition, and rejected on two independent grounds. (1) Magnitude: 8.975 Å is not a numerical tolerance, it is a structurally broken trajectory — plausibly a half-box or one-box minimum-image jump. No written justification can make a box-scale positional disagreement an acceptable bound for rigid-water constraint output. (2) Contract: the frame fixes bit-for-bit loop/vmap equivalence as a hard requirement because Claim-1's entire thesis is that one compiled kernel serves heterogeneous bundles; a documented tolerance on replica disagreement concedes the claim. Retained ONLY as a conditional branch-(b) fallback, admissible if and only if instrumentation shows the residual is sub-Ångström, non-cascading, and bounded — none of which is currently in evidence.
- [DEFER] H1-ELIMINATE (delete the second re-wrap in settle_positions lines 166-175 rather than making it deterministic): Architecturally the cleaner remedy — removes the hazard class instead of patching one instance — but it rests on an unverified assumption that no downstream consumer depends on settle_positions returning strictly in-box coordinates, across all 7 production integrator call sites plus the NPT box-scaling path. Verifying that assumption costs more than the one-line deterministic fix and carries real regression risk against the dt=1.0 fs cap-lift gate. Deferred to a follow-up hardening item; not the right change to make while the root cause is still unconfirmed, since a behavioural change would confound the A/B measurement that is this work item's primary deliverable.
- [MERGE] DETERMINISM-SWEEP (convert every jnp.round-on-box / jnp.mod-on-box site in the physics package in one pass): Merged into the winner in reduced form. The GREP is adopted as a mandatory Stage 0 step and answers backlog scope question 3 (already partially answered by signature inspection: settle_velocities takes no box parameter and is structurally exempt from this bug class). The blanket CONVERSION is not adopted: changing untested sites outside the failing test's blast radius adds regression risk against the dt=1.0fs gate while confounding the A/B measurement. Grep now, enumerate the sites in the work order, convert only settle_positions:171 in this item, and route any additional discovered sites to the deferred hardening item alongside H1-ELIMINATE.
- [MERGE] H2-RNG and H3-EIGH as co-equal hypotheses requiring their own dedicated cluster investigations: Neither is promoted to a standalone investigation, but neither is dropped — the backlog's three-way-bisect requirement is satisfied by making both falsifiable within the winner's plan at near-zero marginal cost. H2 is closed-or-kept by inspection at Stage 0 (read gamma and key-threading config of both failing tests; gamma=0 or keys pre-split outside the scan structurally excludes it). H3 is discriminated by the Stage 0 arithmetic check (8.975 Å against the box vector: box-quantised implicates a wrap site, arbitrary magnitude implicates eigh) and confirmed or refuted by the Stage 1 first-divergence instrumentation, which reads out the primitive error's magnitude signature rather than the amplified cascade. Only if instrumentation implicates them do they become their own backlog items.
- [ACCEPT] INVEST gate — S (Small) dimension, and the three pre-mortem mitigations that must ship as hard constraints: INVEST passes on all six, with S recorded as a boundary pass (7 acceptance criteria against the ≤8 limit, no headroom). Scope control is structural: branch (b) SPLITS #934 rather than absorbing the residual, so H2/H3 are falsifiable here at near-zero marginal cost without growing the item. Breach signal: if a fixer is investigating eigh numerics inside this item, split immediately. Three pre-mortem mitigations are promoted from advisory to hard constraints: (1) the A/B parameterisation must not alter the traced code path — no in-path branch or traced predicate, else the instrument perturbs the ULP-sensitive behaviour under test; fall back to REPRO-FIRST if that cannot be achieved cleanly. (2) Branch (b) must NOT close or mark-resolved #934; hardening lands as a linked non-closing commit, item stays open and re-scoped — this prevents the "defensible-looking partial result" failure where a hardening commit makes a live bug look addressed. (3) The green criterion should prefer one CONSTRUCTED adversarial case (drive a water across a box face on the step SETTLE acts, forcing delta_o/box near ±0.5) over N random seeds; multi-seed is a supplement, not primary evidence.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** Pre-mortem — six months on, this has definitively failed. The most likely story, in order of probability:

FAILURE 1 (most likely): The A/B contaminated its own measurement. To make both rounding idioms reachable in one process, the fixer threaded a flag through settle_positions. That flag changed XLA's fusion boundaries in the constraint path, and the tie-point behaviour under test — which is ULP-sensitive by definition — no longer matched what the shipped code does. Both arms came back green, we took branch (c), closed #934 as already-resolved, and the bug reappeared four months later in a heterogeneous Claim-1 bundle where it silently corrupted a production trajectory. The lesson we should pre-empt: the parameterisation MUST be done without altering the traced code path — a module-level constant swapped between two separate process invocations, or two separate pure helper functions selected at import time, never an in-path branch or a traced predicate. If that cannot be achieved cleanly, fall back to REPRO-FIRST and accept the extra queue round-trip.

FAILURE 2: We landed the hardening, took branch (b), split #934 — and the residual follow-up item never got worked. The "hardening" commit made the item LOOK addressed in the log, the critical-path pressure moved on to the next Claim-1 blocker, and the actual root cause stayed live under a closed-looking ticket. This is the classic failure of a plan whose expected outcome is "necessary but insufficient": it manufactures a defensible-looking partial result. Mitigation: branch (b) must NOT close #934 or mark it resolved; it must leave #934 open and re-scoped, with the hardening committed as a linked but non-closing change.

FAILURE 3: The N>=5-seed green rule was too weak. Five seeds sounds rigorous but a tie-point bug may fire in, say, one trajectory in fifty. We declared green, shipped, and it bit intermittently in production for months as unexplained trajectory blow-ups that nobody connected back to SETTLE. Mitigation: prefer a targeted adversarial seed search over more random seeds — construct a configuration that deliberately places a molecule's delta_o/box near +/-0.5 (i.e. drive a water across a box face on the step where SETTLE acts) and assert determinism THERE. One constructed worst-case beats fifty random draws for a tie-point hazard.

FAILURE 4: Stage 0's arithmetic discriminator returned an ambiguous answer (box neither ~8.975 nor ~17.95 nor clearly large), we shrugged and proceeded to spend the cluster job anyway, and the whole gate did no work. Mitigation: state in advance that an ambiguous 0a result is itself informative and should re-weight toward H3, not be discarded.

FAILURE 5 (lower probability, higher cost): the fix was correct, the tests went green, and six months later we discovered that floor(x+0.5) has its own asymmetry — it is round-half-UP, which is biased away from zero for negative values in a way round-half-even is not. In a long trajectory that systematic bias accumulated into a slow drift in a conserved quantity. This is unlikely at the magnitudes involved but it is the honest counter-risk of the chosen idiom, and it argues for the regression test to assert a conservation property over a long trajectory, not merely loop/vmap agreement.

None of these overturn the winner, but 1, 2 and 3 are serious enough that they should appear as explicit constraints in the handoff, not as afterthoughts.
**AI:** _not recorded_

## Acceptance Criteria
**Given** FIXED (cannot change):

- The physics contract: SETTLE must preserve rigid TIP3P geometry and the molecule's COM, and must not damp genuine molecular rotation (the Horn quaternion rigid fit exists specifically to avoid rotation damping — regressing that reopens the P5 C3 angular-momentum work, 678c9cb).
- The public API surface: settle_positions is called by all 7 production integrator sites (settle_langevin, settle_lfmiddle_langevin x2, settle_with_nhc, settle_csvr, settle_csvr_npt x2). Its signature cannot change.
- Bit-for-bit loop/vmap equivalence is the requirement under test. This is not "close enough" — the whole Claim-1 thesis is that one compiled kernel serves heterogeneous bundles, so a vmap replica must reproduce the looped result.
- The bug's definition includes its environment: manifests at cluster/box scale on Engaging, not locally. Any evidence standard must respect that.
- Project rules: bth run + per-script sidecar for anything in scripts/experiments/; cluster submission via myxcel/sbatch; no whole-suite pytest runs locally (WSL2 box, 29Gi RAM).
- Cannot regress the existing dt=1.0 fs cap-lift validation (gate job 19774893).

NEGOTIABLE:

- Which rounding idiom is used, as long as it is FMA-reassociation-deterministic. floor(x+0.5) is the incumbent because _settle_water_batch already uses it and internal consistency has independent value, but a different deterministic formulation is acceptable if argued.
- Whether the second re-wrap in settle_positions is FIXED (change round -> floor) or ELIMINATED (argue it is redundant given _settle_water_batch already unwrapped, and the constrained output is within a bond length of the unconstrained input). Both are on the table; elimination is a bigger behavioural change but removes the hazard class rather than patching it.
- Verification depth and ordering: which tests, on what hardware, in what sequence, and whether the diagnostic script runs BEFORE the fix (to positively confirm the mechanism) or AFTER (to characterise any residual). This is a real design choice, not a detail.
- Whether hypotheses (2) and (3) get ruled out empirically in this work item or get split into a follow-up backlog item gated on the branch-(b) outcome.
- Scope breadth: minimal single-line change vs a sweep of every PBC-rounding site in settle.py.

ASSUMPTION I want stress-tested, not accepted: that the 8.975 Å figure in the _settle_water_batch docstring refers to the CURRENT (post-fix-in-that-function) state of the test rather than the historical pre-fix state. If that number was recorded BEFORE the floor() fix landed in _settle_water_batch and nobody re-ran the cluster test afterwards, then we do not actually know the bug still reproduces — and the first action is a reproduction run, not a patch. The framing "incomplete fix" presumes the test is still red; that presumption is unverified.
**When** implementing STAGED-BISECT (amended): Stage 0 free discriminators, then a SINGLE pipelined instrumented A/B cluster job carrying the settle_positions:171 determinism fix as hardening — with an asymmetric red/green evidence rule.

FRAMING (load-bearing, do not soften): the one-line fix is landed as obviously-correct hardening, NOT as the confirmed cause. Assumption-reversal analysis argues site 171 is probably COLD: _settle_water_batch:319 rounds a full integrator-step displacement (delta/box ~= +/-0.5 at every genuine PBC face crossing — sitting exactly on the banker's-rounding tie point), whereas settle_positions:171 rounds a sub-Angstrom SETTLE geometric correction (delta_o/box ~= 0.01 for a 10 A box, far from the tie point). The two sites are NOT symmetric hazards, so the "incomplete fix" story is plausible but unproven. The primary deliverable of this work item is therefore the MEASUREMENT (which site fires first; is the primitive error box-quantised), not the patch. A fixer must not declare victory on a green run the fix did not cause.

STAGE 0 — zero compute, all three are blocking prerequisites:
(0a) ARITHMETIC DISCRIMINATOR: read the box vector used by tests/physics/test_settle_batched_vs_unbatched and test_settle_64water_batched_10ps.py. Compare 8.975 A against it. box ~= 17.95 -> half-box -> minimum-image SIGN disagreement (H1 wrap family). box ~= 8.975 -> clean one-box jump (H1 wrap family). box >= 30 -> 8.975 is arbitrary, wrap story is WEAK, re-weight toward H3/eigh before spending the cluster job.
(0b) H2 CLOSE-BY-INSPECTION: read gamma and RNG key-threading config of both failing tests. If gamma=0, or per-replica keys are pre-split OUTSIDE the jax.lax.scan, H2-RNG is structurally excluded at zero cost — record and close. If gamma>0 with in-scan key threading, H2 stays live and gets a gamma=0 control arm added to the Stage 1 job (divergence vanishes -> H2 implicated; persists -> H2 excluded).
(0c) SCOPE GREP: `rg 'jnp\.round\(|jnp\.mod\(' src/prolix/physics/settle.py` and inspect every hit for a box-normalised argument. Already established by signature inspection: settle_velocities (line 850) takes NO box parameter and is structurally exempt. Enumerate any additional sites in the work order; convert only :171 in this item, route the rest to the deferred hardening item.

THE FIX (hardening, landed regardless of outcome):
  File: src/prolix/physics/settle.py, line 171, inside settle_positions
  OLD: image_correction = -box * jnp.round(delta_o / box)
  NEW: image_correction = -box * jnp.floor(delta_o / box + 0.5)
Rationale to put in the commit message: jnp.round is round-half-to-EVEN (banker's); under XLA FMA contraction a value at ULP proximity to +/-0.5 can round in opposite directions on the loop vs vmap path. floor(x+0.5) is deterministic round-half-up under any FMA reassociation order. This mirrors the idiom already used at _settle_water_batch:319 and makes the file internally consistent. Also update the stale comment block at lines 145-165, whose line 161 currently ARGUES FOR round() ("The only exception is a genuine PBC crossing (|delta| ~ box), handled by round()") — that sentence names precisely the regime where banker's rounding is unsafe and must not survive the change.

STAGE 1 — ONE cluster job, within-job A/B (pipelining Stages 1+2 to spend one queue round-trip, not three, on a Claim-1 critical-path item):
Extend scripts/experiments/p5_settle_batch_divergence.py (do NOT write a new diagnostic) to parameterise the rounding idiom so pre-fix and post-fix code paths run in the SAME process, SAME seed, SAME box. This makes it a true A/B where the only varying quantity is the intended one — eliminating the ambiguity where a green post-fix run merely reflects a different machine or seed.
Metric change (critical): report FIRST-DIVERGENCE STEP and the per-molecule delta at that step, NOT terminal-trajectory RMSD. A single wrap disagreement produces a +/-box error, hence a huge spurious force, hence a blown-up replica — so terminal RMSD measures the amplified cascade, not the primitive error, and is near-useless for root-causing. At first divergence, dump: the offending molecule's pre/post positions, delta_o/box, and whether |delta| is near an integer-or-half-integer multiple of box.
Run under `bth run` with a per-script sidecar (`p5_settle_batch_divergence.bth.toml`, [experiment] schema, exactly one outcome with is_residual=true) inside a slurm wrapper sourcing scripts/slurm/_bth_env.sh; register the campaign via `bth campaign create` BEFORE the first run and pass --campaign to every run.

EVIDENCE RULE (asymmetric, declared in advance, encoded in the sidecar outcomes):
  RED is conclusive on a single observation — a tie-point bug that fires proves the hazard is reachable.
  GREEN is NOT conclusive on a single observation — an FMA tie-point bug is a heisenbug by construction, firing only when a value lands close enough to +/-0.5, which is seed- and trajectory-dependent. Pass criterion is N>=5 seeds all under tolerance. Cheap, because the seeds run within the same job.

BRANCH ACTIONS (pre-declared so no result can be read post-hoc):
  (a) Post-fix arm green across >=5 seeds AND pre-fix arm red -> fix is sufficient and CAUSAL. Land it, add a cluster-marked regression test, close #934.
  (b) Both arms red -> fix is necessary-but-insufficient (the expected case under the cold-site analysis). Land the hardening anyway, and read the first-divergence instrumentation: box-quantised primitive error -> a wrap site we have not found (feed the Stage 0c grep list); arbitrary-magnitude error -> H3/eigh. Split #934, route the residual to a targeted follow-up.
  (c) BOTH arms green across >=5 seeds -> the bug was already resolved by the _settle_water_batch fix and the 8.975 A docstring records the historical PRE-fix state. Close #934 as already-resolved, land the hardening, and add the cluster-marked regression test so it cannot silently regress.

CLUSTER RE-VALIDATION IS MANDATORY. The bug is DEFINED as "passes locally, fails on Engaging" — a local green run is not evidence of anything and must not be accepted as closure. Submit via myxcel/sbatch per project rules (verify partition/walltime flags are actually inherited; --time/--partition non-inheritance bug observed 2026-08-11). Do not run the 20k-step suite on the local WSL2 box.
Regression guard: confirm the dt=1.0 fs cap-lift gate is not disturbed (reference job 19774893) before merging.
**Then**
  - [ ] _add specific measurable criteria_
