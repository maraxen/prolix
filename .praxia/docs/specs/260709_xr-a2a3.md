---
title: "XR-A2A3 — Sprint A exclusions + settle-dt"
backlog_id: XR-A2A3
praxia_id: 3273
epic: 260709_xtrax_rewire
depends_on: []
priority: P1
difficulty: extended
status: completed
challenge_verdict: pass
challenge_summary: "A1 masses+A2 excl C1 green; A3 C3 via XR-VACUUM-DT exception (dt=0.5 vacuum not achievable; SETTLE-only falsified)."
completed_2026_07_09: true
---

# XR-A2A3

## Goal
Finish Sprint A physics path on **main**: A1 masses (port from worktree if missing), A2 JIT-safe exclusions through the B1 keystone loader, A3 vacuum-protein settle-dt root-cause — sibling to tiling rewire; unblocks XR-PARITY-OMM-PROTEIN.

## Normative parent
[`.praxia/docs/specs/260706_b1-core-md-path-fix.md`](260706_b1-core-md-path-fix.md) (copied from xtrax-030-cutover worktree).

## Locked decisions (challenge revise 2026-07-09)

| ID | Topic | Lock |
|----|-------|------|
| C1 | Force-scale gate | After `_b1_paramize.paramize_pdb_to_bundle("data/pdb/2GB1.pdb")`, compute forces via `energy_fn_from_bundle` + `jax.grad` on **real atoms only** (`atom_mask`). **Pass:** `median(|grad|) < 1e3` kcal/mol/Å and finite energy. Committed as keystone self-test / pytest (not a one-off manual run). |
| C2 | V4/V5 | **In scope as must-stay-green.** On main, V4-HLO + V5 already pass (JIT-safe `_dense_excl_matrices_from_bundle` scatter). A2 must not regress `tests/api/test_v4_hlo_hetero_compile_once.py` + `tests/api/test_v5_observable_parity.py` (`-m "not slow"`). |
| C3 | A3 done bar | **Both:** (1) written root-cause + dt/integrator policy in `.praxia/docs/research/`; (2) **either** 2GB1 finite ≥1000 steps at `dt=0.5` fs via `EnsemblePlan` **or** documented policy exception + new backlog leaf that `XR-PARITY-OMM-PROTEIN` depends on. Docs-only without (2) is **not** done. |
| C4 | Exclusion representation | (a) Host: `ExclusionSpec.from_protein(protein)` → `make_bundle_from_system(..., exclusion_spec=...)` which calls `map_exclusions_to_dense_padded` then stores **pair-list** `excl_indices (E,2)` + scales/mask on the bundle. (b) Device: `_dense_excl_matrices_from_bundle` builds dense `(N,N)` via **JIT-safe scatter** from those fields (already on main — no numpy/`int(n_excl)` loop). Do **not** rebuild `ExclusionSpec` under trace. |
| C5 | Pre-A2 baseline | Main tip at leaf start; allowlist empty for V4/V5 (already green). Fast suite: `tests/api tests/bench tests/tiling tests/batching -m "not slow"`. |
| C6 | AC4 | Deleted as process bookkeeping; PROT stays `depends_on: [XR-A2A3, XR-KILL-FORK]` in backlog. |
| C8 | B1-full | Finite protein `EnsemblePlan` at pinned dt is **A3 verify** (C3), not Claim-1 throughput. Throughput remains out of scope. |
| A1 | Masses | Port worktree A1 (`MolecularBundle.masses` + pure-slice `masses_for_bundle`) onto main as part of this leaf before claiming protein MD correct. |

## Scope
- **A1:** `MolecularBundle.masses`; `make_bundle_from_system` pad; `masses_for_bundle` pure slice
- **A2:** Wire `ExclusionSpec.from_protein` in `scripts/benchmarks/_b1_paramize.py`; keep JIT scatter path; C1 force gate
- **A3:** Investigation write-up + C3 trajectory-or-exception

## Non-goals
Tiling fork kill; Claim-1 throughput headline; implementing proxide XTC write.

## Acceptance Criteria
1. Given 2GB1 from `_b1_paramize`, when forces computed on real atoms, then `median(|grad|) < 1e3` kcal/mol/Å and energy finite.
2. Given A1+A2 changes, when `tests/api/test_v4_hlo_hetero_compile_once.py` + `tests/api/test_v5_observable_parity.py` run with `-m "not slow"`, then they pass; no new failures in the fast api/bench/tiling/batching suite vs tip baseline.
3. Given A3, when complete, then `.praxia/docs/research/` contains root-cause + policy **and** C3 trajectory gate or exception leaf is filed.
4. Given A1, when a protein bundle is built, then oxygen mass ≈ 16 (not 1.0) on real atoms.

## Rollback
Revert A1+A2 commits as a unit; keep A3 docs. Verify V4/V5 + fast suite after revert.

## References
- Parent: `260706_b1-core-md-path-fix.md`
- Worktree handoff: `.claude/worktrees/xtrax-030-cutover/.praxia/handoffs/xtrax-030-cutover_sprintA-a1-md-path-fix_20260706_handoff.yaml`
- `ExclusionSpec.from_protein`, `map_exclusions_to_dense_padded`, `make_bundle_from_system(..., exclusion_spec=)`
