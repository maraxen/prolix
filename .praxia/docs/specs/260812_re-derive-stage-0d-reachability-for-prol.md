---
session_id: 8e95b44d
topic: Re-derive Stage 0d reachability for prolix backlog #934 (P5-C): can any of the 5 unconverted PBC-rounding sites in settle.py physically land within 1 ULP of an exact half-integer of delta/box, and if not, what should Stage 1 actually measure?
task_type: diagnostic
winner: 0d-UNREACHABLE AT ALL SIX SITES; BRANCH (d) FIRES; STAGE 1 IS RE-SPECIFIED AS A DISCONTINUITY-PROXIMITY TEST, NOT A ROUNDING-IDIOM A/B.

=== 1. CORRECTED STAGE 0d TABLE (replaces session fd78460a's) ===
Invariant (derive it, do not inherit it): every argument to a box-normalised rounding site in settle.py has the form delta = s + k*box + eps, k integer, |eps| ~ 1e-15 A, because the only non-physical coordinate motions in the call graph — shift_fn / jnp.mod wraps and settle_positions' image_correction — add EXACT INTEGER multiples of box. Nothing adds a half box. So delta/box = k + s/box, and round()-vs-floor(x+0.5) can only disagree if |s| = box/2 = 5.0 A (box = [10,10,10] from test_settle_batched.py L72).
  L188  settle_positions delta_o        s = SETTLE geometric correction to O, <~0.05 A   ->  s/box <~ 0.005   UNREACHABLE (100x margin)
  L336  _settle_water_batch unwrap      s = one A-step displacement v*dt/2 at dt=0.5 fs, <~0.02 A  ->  <~0.002  UNREACHABLE (250x margin; ALREADY CONVERTED, and the LEAST reachable of the six)
  L790  dH1 = r_H1_unc - r_O_unc        s = TIP3P O-H bond vector, PINNED by the constraint at 0.9572 A  ->  0.0957  UNREACHABLE (5.2x margin; tie requires box = 2*r_OH = 1.914 A)
  L793  dH2                             identical to L790                                              UNREACHABLE
  L1294 dx_1 = x_con_1 - x_unc_1        O rows: the L188 min-image residual <~0.05 A. H rows: SETTLE correction <~0.1 A PLUS an exactly-integer image offset. Non-water rows: identically 0.  UNREACHABLE
  L1346 dx_2                            identical to L1294                                            UNREACHABLE
OVERALL: 0d-UNREACHABLE. Prior REACHABLE verdicts for L790/793 and L1294/1346 are REVERSED. The prior error was treating the attainable range as a continuous interval that "includes 0.5"; the attainable SET is a union of narrow clusters around integers {k +/- 0.1}, and 0.5 lies in the GAP between clusters. L1294/1346 is NOT structurally different from L790/793 in the way that matters — "same atom computed two ways" still differs only by (bounded SETTLE correction) + (integer image offset), because image_correction is by construction an integer multiple of box.
Loopholes probed and closed: NPT box rescaling (needs 25-33% linear box change in ONE step; both Stage 1 targets are NVT anyway); unphysically small box (the only exactly-solvable condition, box = 1.914 A); anisotropic box (invariant is per-component); solute rows (identically zero); pre-detonated state (makes the site a symptom, not a cause).

=== 2. THE MECHANISM THAT SURVIVES: H-DISCONTINUITY ===
The spec conflated two different points. For f(x) = x - box*ROUND(x/box) the output jumps by a full box wherever x/box crosses a half-integer, and round() and floor(x+0.5) place that jump at the SAME location — they differ only in which side the exact boundary point falls on. So:
  - "Do the two IDIOMS disagree?" is the tie question: measure ~1 ULP, UNREACHABLE everywhere in settle.py.
  - "Do the loop and vmap PATHS disagree?" is the discontinuity question: it fires when x/box lands within 1 ULP of a half-integer AND the two paths' x differ by >= 1 ULP. It requires NO rounding-mode difference and is NOT fixed by converting the idiom.
FIXTURE PATHOLOGY: small_water_system puts the oxygen at EXACTLY [0,0,0] (a box corner) and the solute at EXACTLY [5,5,5] = box/2 in every component, with box = [10,10,10]. 5.0/10.0 = 0.5 exactly; 0.0/10.0 = 0.0 exactly. Whichever wrap convention applies, one of those atoms sits precisely ON a wrap discontinuity — and BOTH are harmonically restrained to those exact points (k = 0.5 kcal/mol/A^2, positions_ref identical to the initial coordinates), so their probability density at the discontinuity is a Gaussian CENTRED on it (sigma = sqrt(kT/k) ~ 1.1 A). Density at the discontinuity is MAXIMAL. That is the exact inverse of every settle.py site, where it is identically zero.
MAGNITUDE CHECK (new; the prior sessions could not do this): compute_rmsd (test L250-252) averages over ALL 12 scalars, not per-atom distances. One box (10 A) applied to the 3 water atoms in all 3 components gives sqrt(9*100/12) = 8.660 A. Observed: 8.975 A (3.6% high, residual NOT accounted for — flagged). By contrast full decorrelation of this harmonically-bounded system saturates at ~1.5 A, and even uniform-in-box positions give only 4.08 A. So 8.975 A is BOX-QUANTISED, not chaotic amplification. Stage 0a therefore resolves to ONE-BOX JUMP / H1-wrap-family CONFIRMED — the spec's 0a table reached that conclusion by wrong arithmetic (it compared an RMSD normalised over 12 scalars directly against a box length, and assumed box = 17.95).
Honest limitation: H-DISCONTINUITY is an AMPLIFIER hypothesis, not a SOURCE hypothesis. It explains the magnitude and the fixture; it does not explain why the two paths differ by an ULP in the first place, nor why the test passes locally (which requires assuming CPU emits bit-identical loop/vmap code and GPU does not).

=== 3. REVISED STAGE 1 — three arms, one job, no tie-point construction ===
G1 as originally conceived is DROPPED (void, unsatisfiable). Replacements, in priority order:
  (S0-FIRST, zero compute, BLOCKING) Read _langevin_step_a's body and record how shift_fn is invoked. The fixture's _shift_fn(dR, box) = dR - box*jnp.round(dR/box) is a SEVENTH rounding site that no prior enumeration covered because 0c was scoped to settle.py. Whether its first argument receives an absolute position (discontinuity at +/-box/2 = the solute) or a displacement decides which atom is on the discontinuity. Do this before spending any cluster time.
  (G1'-TRANSLATION, PRIMARY) Re-run the identical test with every position rigidly translated so no atom sits on or near a wrap discontinuity — e.g. O at [3.1,3.3,3.7], H1/H2 translated identically, solute at [7.2,6.9,7.4] — same seed, box, dt, gamma, step count. Rigid translation of a periodic system is a physical symmetry, so the dynamics are equivalent and only discontinuity-proximity changes. PREDICTS: RMSD collapses from ~8.975 A to ~1e-15 A if H-DISCONTINUITY holds; persists at ~8.9 A if it does not, killing the wrap-family story outright. Hand-constructible, deterministic, one fixture edit.
  (G1''-BITEXACT, PRIMARY) At step 0, dump x = delta/box at all six sites on both paths and compare BITWISE (.view(int64)). Bit-identical everywhere -> no rounding site can ever differ and the entire rounding investigation closes. Differing by 1 ULP -> H-LOWER is live and the site is NAMED. Differing by >> ULP -> something upstream is already wrong. This tests the PRECONDITION for any rounding divergence, which is the honest replacement for "prove the hot regime is deterministic".
  (G1'''-IDIOM-A/B, demoted to CONTROL) PROLIX_PBC_ROUND_MODE=round vs floor back-to-back, import-time selection, same node/seed/SHA. Costs one env var. Directly tests whether the L336 conversion ever fixed anything. NOT primary: under H-DISCONTINUITY both idioms are predicted to fail identically.
  (G2, retained) N >= 5 seeds under TOL. Unchanged and still mandatory as evidence that no other regime is hot.
SCALE: run the SMALL target (test_settle_batched.py::test_settle_batched_vs_unbatched, 4 atoms, 10 steps) as the primary vehicle. The divergence is a single discrete event, not an exponential ramp, so the 20k-step 64-water run is NOT required to observe it and should not be the first spend. This also sidesteps the #4193 hang budget almost entirely.
EVIDENCE RULE: RED stays conclusive on a single observation. GREEN now requires G1'-TRANSLATION + G1''-BITEXACT + G2. G1'''-IDIOM-A/B is reported, never gating.

=== 4. BRANCH SELECTION ===
Branch (d) FIRES — but with a correction to how the spec frames it. The spec expected branch (d) to mean "the tie-point mechanism is falsified, re-scope to H2/H3." The actual finding is stronger and more specific: the tie-point mechanism is falsified AND a concrete replacement mechanism (H-DISCONTINUITY, rooted in a pathological test fixture rather than in library code) is identified, with a cheap constructible discriminator. The five conversions stay landed as consistency-only hardening on a NON-CLOSING commit; #934 stays OPEN; the positive-check gate applies in full — a new backlog item must be FILED with a recorded ID carrying this 0d table, the corrected 0a arithmetic, and the S0-FIRST shift_fn finding, and #934's re-scope note must link to it. UNREACHABLE is not a tidy ending and must not be written up as one.
created_at: 2026-08-12T17:56:54.098263+00:00
---

# Brainstorm: Re-derive Stage 0d reachability for prolix backlog #934 (P5-C): can any of the 5 unconverted PBC-rounding sites in settle.py physically land within 1 ULP of an exact half-integer of delta/box, and if not, what should Stage 1 actually measure?

## Problem Frame
FIXED (cannot change):
- The landed hardening commits 8867216 / 091a83a stay. floor(x+0.5) as the project-wide PBC idiom is not up for reversal in this session.
- settle.py's physics semantics: minimum-image at each of the 6 sites must keep doing what it does; we are only reasoning about which rounding tie-break fires and when.
- The bug's definition: "passes locally (WSL2/CPU), diverges on Engaging (GPU)". Any evidence path that only produces a local green proves nothing.
- Float64 (x64 enabled in the target test). ULP at |x|~1 is 2^-53 ≈ 1.1e-16.
- shift_fn / jnp.mod only ever add INTEGER multiples of box to a coordinate. This is the load-bearing invariant of the whole re-derivation and must be stated, not assumed.
- Cluster budget: at most one job. Backlog #4193 hang risk applies.

NEGOTIABLE:
- Stage 0d's verdict itself (that's the point of this session).
- G1's definition, or its existence. The spec's own REVERSAL B already anticipated that G1 could become unsatisfiable; I am willing to drop it outright and say so.
- Which test/scale Stage 1 runs at, and how long. The 20k-step 64-water run is not sacred.
- The metric. max_abs_atom_position_delta per step is probably right but the GATE built on it may not be.
- Whether the causal story in settle.py's own docstring (L309-316: "floor(x+0.5) gives deterministic round-half-up regardless of FMA reassociation order ... producing an 8.975 Å RMSD") survives. I suspect this comment is itself an unverified claim and possibly numerically wrong as stated.
- Whether TOLERANCE-DOC deserves partial rehabilitation — not as "loosen and move on", but as a testable claim about what a B=1-vmap-vs-unbatched comparison can possibly guarantee on GPU.

Stress-test assumption I most want attacked: that "delta = (bounded small physical quantity) + (exact integer multiple of box)" holds at ALL five sites, making delta/box = integer + small/box and therefore never within 1 ULP of a half-integer unless small ≈ box/2 = 5 Å. If that invariant has a hole, the whole UNREACHABLE verdict collapses.

## Idea Pool
- [user] H-TIE (the prior session's hypothesis): loop and vmap paths compute delta/box such that it lands within 1 ULP of an odd-numerator half-integer (0.5, 2.5, ...), where round-half-to-even and round-half-up disagree, producing a full-box output jump. Requires: some site's delta can physically equal box/2 = 5.0 Å to within ~1e-15 Å.
- [user] H-LOWER (spec REVERSAL A(1)): the two idioms lower to different XLA instruction sequences; the extra add changes fusion/FMA-contraction boundaries, so the UPSTREAM value x = delta/box itself is computed differently, and the two forms disagree by an ULP anywhere, not only at ties. Requires: XLA's fusion of the surrounding expression is sensitive to the op-graph shape at this site, AND that ULP difference is amplified downstream.
- [user] H-ILLPOSED (new, named by neither v1 nor v2): unbatched apply_fn and jax.vmap(apply_fn) on a B=1 batch are NOT required by XLA to lower to bit-identical kernels. On GPU they demonstrably do not (different tiling, different reduction order, different fusion). So a ~1e-16 Å difference at step 0 is EXPECTED, not a bug; Langevin+chaotic dynamics amplify it to O(box) over enough steps; the test's terminal RMSD < 1e-10 Å gate is therefore unachievable on GPU independent of any rounding site. Requires: step-0 loop-vs-vmap difference is nonzero on GPU, and amplification over the test's trajectory length is sufficient to reach O(1) Å.
- [user] Three hypotheses recorded (H-TIE, H-LOWER, H-ILLPOSED). Now the derivation that discriminates them.
- [user] INTEGER-LATTICE INVARIANT — the core argument, stated so it can be attacked.
- [user] Every quantity fed to a rounding site in settle.py has the form
- [user] delta = s + k*box + eps,  k integer, |eps| ~ ULP(coordinate) ~ 1e-15 Å
- [user] where s is a BOUNDED PHYSICAL quantity. Reason: the only operations that ever move a coordinate by a non-physical amount are shift_fn / jnp.mod wraps and the settle_positions image_correction, and BOTH add exact integer multiples of box (to within one rounding, hence eps). Nothing in the call graph ever adds a HALF box.
- [user] Therefore delta/box = k + s/box + O(1e-16), and a tie requires |s| ≈ box/2 = 5.0 Å.
- [user] Per-site bound on s (box = 10 Å, dt = 0.5 fs, TIP3P):
- [user] L188 delta_o = pos_O_c - pos_O_new. s = the SETTLE geometric correction to the oxygen. SETTLE only removes bond-length/angle error accumulated in one A-step; |s| <~ 0.05 Å. s/box <~ 0.005. Tie needs 100x more.
- [user] L336 unwrap delta = pos_new - pos_old, same atom, one A-step apart. s = v*dt/2. At 300 K a TIP3P hydrogen's rms speed gives |s| <~ 0.02 Å at dt=0.5 fs. s/box <~ 0.002. Tie needs 250x more. NOTE: this is the ALREADY-CONVERTED site whose conversion supposedly fixed the bug — and it is the LEAST reachable of the six.
- [user] L790/L793 dH = r_H_unc - r_O_unc. s = the true O-H bond vector of a rigid TIP3P water = 0.9572 Å exactly (constrained, not free). s/box = 0.09572. Tie needs |s| = 5.0 Å, i.e. 5.2x the bond length. The bond is a CONSTRAINT — SETTLE enforces it every step — so s is not merely bounded, it is PINNED. This is the strongest UNREACHABLE of the five.
- [user] L1294/L1346 dx = x_con - x_unc, per atom.
- [user] Oxygen rows: x_con's image was chosen by L188 to be the minimum image of x_unc's oxygen, so dx_O IS the L188 min-image residual, |s| <~ 0.05 Å.
- [user] Hydrogen rows: pos_h_c sits near H's OWN old image (L336 unwraps each atom against its own pos_old) and then receives the OXYGEN's rigid integer image shift; x_unc's H was wrapped independently. So dx_H = (SETTLE correction to H) + (difference of two integer image indices)*box. |s| = SETTLE correction to H <~ 0.1 Å; the integer part is exactly that — integer. s/box <~ 0.01.
- [user] So the prior session's REACHABLE verdict for L790/793 and L1294/1346 was an artifact of describing the range as a continuous interval "[0, 1.0]" when the attainable SET is a union of narrow clusters around integers: {k ± 0.1}, k ∈ {-2..2}. 0.5 falls in the GAP between clusters, not in the set. Range-inclusion was the wrong test; the set is not an interval.
- [user] This confirms and sharpens the user's physical reasoning about L790/793 — but it also EXTENDS it to L1294/L1346, which the user flagged as structurally different. It is not structurally different in the way that matters: the "same atom computed two ways" path still differs only by (bounded SETTLE correction) + (integer image offset), because settle_positions' image_correction is by construction an integer multiple of box.
- [critic] LOOPHOLE-1 (NPT box change): the invariant assumes ONE box value across all wraps contributing to a given delta. Under settle_csvr_npt the box is rescaled every step. If a coordinate was wrapped under box_t and delta is normalised by box_(t+1), then delta/box = k*(box_t/box_(t+1)) + s/box_(t+1). A tie at k=1 needs box_t/box_(t+1) = 1.5 (33% linear compression in ONE step); at k=2, ratio 0.75 or 1.25 (25% in one step). Both are orders of magnitude beyond any physical barostat step. VERDICT: real hole in the invariant's generality, but quantitatively closed by 2+ orders of magnitude, and irrelevant to the two Stage 1 target tests which are NVT with a fixed box.
- [critic] LOOPHOLE-2 (unphysically small box): L790/793's s is PINNED at r_OH = 0.9572 Å. A tie needs s = box/2, i.e. box = 2*r_OH = 1.9144 Å (or box = 2*r_OH/(2k+1) for higher ties: 0.638 Å, 0.383 Å...). This is the ONLY exactly-solvable reachability condition in the whole analysis, and it demands a box smaller than a single water molecule. VERDICT: this is precisely the "engineer a bit-exact G1 case" option the user rejected, and it is now shown to require an unphysical configuration. That rejection is retroactively vindicated on quantitative grounds, not just taste.
- [critic] LOOPHOLE-3 (non-cubic / anisotropic box): box is a (3,) vector and each component divides its own delta component. Doesn't help — the invariant is per-component, so a tie in x needs s_x = box_x/2 with box_x still 10 Å. VERDICT: no hole.
- [critic] LOOPHOLE-4 (a solute atom, not water, reaching a site): L1294/L1346 apply dx to ALL atoms including the harmonically-restrained solute, not just waters. But the same decomposition holds: for a non-water atom settle_positions leaves the position untouched, so dx = x_unc - x_unc = 0 exactly. VERDICT: no hole; solute rows are identically zero, hence maximally far from a tie.
- [critic] LOOPHOLE-5 (a NaN/blown-up upstream state): if the integrator has already detonated, s is unbounded and anything is reachable. VERDICT: real but self-defeating — it makes the rounding site a symptom, not a cause, and by then the trajectory is already lost.
- [user] Five loopholes probed; none opens. Now two numerics corrections that cut against MY OWN framing and against the user's, and which change what Stage 1 should do.
- [user] CORRECTION 1 — the user's empirical claim is slightly too strong, and the correction matters.
- [user] The user sampled x from 0.499990 to 0.500010 in steps of 2e-6 and found disagreement only at x = 0.500000. But 2e-6 is ~1e10 ULPs; that sweep cannot see the ULP-scale structure. Worked analytically:
- [user] Let x = 0.5 - 2^-54 (the double immediately below 0.5). Exact x + 0.5 = 1 - 2^-54. Doubles near 1 from below are spaced 2^-53, so 1 - 2^-54 is EXACTLY halfway between 1 - 2^-53 and 1.0; round-to-nearest-even picks 1.0. So floor(x + 0.5) = 1, while round(x) = 0. THEY DISAGREE ONE ULP BELOW THE TIE, not only AT it.
- [user] So the disagreement set is {0.5 - 2^-54, 0.5} ∪ {2.5 - 2^-52, 2.5} ∪ ... — still ~1 ULP wide, so the reachability verdict is unchanged, but the mechanism is DOUBLE ROUNDING in the add, not tie-breaking alone. This must go in the artifact because it is the kind of overstatement that gets a finding overturned later.
- [user] Corollary that matters more: floor(x+0.5) is, in the pure-numerics sense, LESS accurate than round(x) — it inherits a double-rounding error that round() does not have. Its virtue is convention/determinism-by-fiat, not accuracy. Any commit message or docstring claiming floor(x+0.5) is "more correct" is wrong.
- [user] CORRECTION 2 — PURE-FUNCTION SYMMETRY. This is the argument that actually kills the docstring's causal story.
- [user] round(x) is a pure, exactly-specified function of the float64 bits of x. floor(x+0.5) is ALSO a pure function of the bits of x. Consequences:
- [user] (a) If x is bit-identical on the loop and vmap paths, BOTH idioms return bit-identical results. Neither can rescue anything.
- [user] (b) If x differs by 1 ULP between paths, round() disagrees only when that ULP straddles a tie (measure ~1 ULP), and floor(x+0.5) disagrees only when that ULP straddles a tie (measure ~1 ULP). SAME measure. Neither idiom is more robust to x differing.
- [user] Therefore settle.py L310-316's claim — "floor(x + 0.5) gives deterministic round-half-up regardless of FMA reassociation order" — is FALSE AS WRITTEN. FMA reassociation perturbs the computation of x UPSTREAM of the round; both idioms then consume the same perturbed x. Determinism with respect to reassociation is not a property either idiom has.
- [user] The ONLY way the L336 conversion could have changed a GPU result is by changing how x itself is computed — i.e. H-LOWER, not H-TIE. The docstring asserts H-TIE and is self-refuting: it cites banker's rounding while describing an FMA-reassociation mechanism that banker's rounding is orthogonal to.
- [user] This inverts the weighting the whole spec was built on. H-TIE is not merely unreachable at all five unconverted sites — it is unreachable at the ALREADY-CONVERTED site L336 too (s <~ 0.02 Å vs the 5.0 Å needed, the widest margin of any of the six). So the spec's branch (d) open question ("if the tie point was never reachable at L319/L336 either, WHY did converting it fix an observed cluster divergence?") is now the LIVE question, and it has exactly three candidate answers.
- [ai] THREE CANDIDATE ANSWERS to "why did converting L336 appear to fix the cluster divergence, if the tie is unreachable there too":
- [ai] (A1) FUSION-COINCIDENCE: adding an op changed XLA's fusion/contraction decisions for the surrounding expression, so delta and delta/box happen to be computed by the same instruction sequence on the loop and vmap paths where previously they were not. The fix is real but INCIDENTAL and un-warranted — a jaxlib/XLA upgrade, a shape change, or a new fusion heuristic can silently undo it. Predicts: the fix is fragile and version-pinned; and an equivalent no-op perturbation (e.g. delta - box*round(delta/box + 0.0)) would work just as well.
- [ai] (A2) NEVER-FIXED / MISATTRIBUTED: the conversion did not actually fix anything; the test's pass was caused by something else in the same commit, a different node/driver, a shorter run, or was never A/B'd at all. The 8.975 Å figure lives in a DOCSTRING, not in a recorded A/B result. Predicts: re-running the pre-conversion idiom today via PROLIX_PBC_ROUND_MODE=round reproduces nothing.
- [ai] (A3) TIE-AT-A-SITE-NOT-ENUMERATED: some rounding/mod site OUTSIDE settle.py (jax_md's own shift_fn/periodic displacement, the neighbor list, or pbc.py) has a genuinely half-box-scale delta — inter-atomic distances DO reach box/2 routinely, unlike every intramolecular site in settle.py. Predicts: the 0c grep scope was too narrow; it should cover prolix.physics.pbc and the jax_md space/shift functions, where minimum-image is applied to genuine INTER-atomic separations whose distribution has real density at box/2.
- [ai] A3 IS THE ONE THE ENTIRE SPEC MISSED. Every settle.py site is intramolecular or same-atom, hence integer-clustered. But any minimum-image applied to a PAIR SEPARATION (energy, forces, neighbor list) has a smooth, non-vanishing probability density exactly at box/2 — that is where the tie is genuinely reachable. If the rounding story is true anywhere in this codebase, it is there, not in settle.py.
- [ai] H-DISCONTINUITY (new; supersedes H-TIE as the live wrap-family mechanism). The tie point and the DISCONTINUITY are different points, and the spec conflated them. For f(x) = x - box*ROUND(x/box), the output jumps by a full box wherever x/box crosses a half-integer. round() and floor(x+0.5) place that jump at the SAME location — they differ only in which side the exact boundary point itself falls on (a set of measure ~1 ULP). So:
- [ai] Whether the two IDIOMS disagree is the tie question: measure ~1 ULP, and UNREACHABLE at all six settle.py sites.
- [ai] Whether the loop and vmap PATHS disagree is the discontinuity question: it fires whenever x/box lands within 1 ULP of a half-integer AND the two paths' x differ by >= 1 ULP. Same 1-ULP window — but this one does NOT require any rounding-mode difference, and it is not fixed by converting the idiom, because both idioms have the identical discontinuity.
- [ai] FIXTURE PATHOLOGY, and why the density at that window is maximal rather than vanishing: test_settle_batched.py's small_water_system places the oxygen at EXACTLY [0.0, 0.0, 0.0] — a box corner — and the solute at EXACTLY [5.0, 5.0, 5.0] = box/2 in every component. box = [10,10,10]. Both are exact float64 values; 5.0/10.0 = 0.5 EXACTLY, and 0.0/10.0 = 0.0 EXACTLY. Whichever wrap convention the caller uses, one of these two atoms sits precisely ON a wrap discontinuity: a [0,box) jnp.mod wrap is discontinuous at 0 (the oxygen), and a [-box/2,box/2) minimum-image wrap x - box*ROUND(x/box) is discontinuous at +/-box/2 (the solute). AND BOTH ATOMS ARE HARMONICALLY RESTRAINED TO THOSE EXACT POINTS (k = 0.5 kcal/mol/A^2, positions_ref = the same coordinates), so their probability density is a Gaussian CENTRED ON THE DISCONTINUITY with sigma = sqrt(kT/k) ~ 1.1 A. The density at the discontinuity is therefore maximal, not vanishing. This is the exact inverse of the situation at every settle.py site, where the density at the discontinuity is identically zero.
- [ai] AMPLIFICATION PATH that matches the observed number: if the oxygen's wrap flips between the loop and vmap paths, settle_positions computes a different delta_o, and the resulting image_correction (L188-192) is applied RIGIDLY TO ALL THREE WATER ATOMS and to no one else. That is a coherent one-box displacement of exactly 3 of the 4 atoms.
- [ai] QUANTITATIVE 0a RESOLUTION (now computable; the prior sessions could not do this because they lacked box=10, N=4, and compute_rmsd's definition). compute_rmsd (test_settle_batched.py L250-252) = sqrt(jnp.mean((p1-p2)**2)) — the mean is over ALL 12 scalars (4 atoms x 3 components), NOT over per-atom distances. Therefore:
- [ai] 3 water atoms displaced by one box (10 A) in all 3 components: sqrt(9*100/12) = 8.660 A.
- [ai] 3 water atoms, 2 components: sqrt(6*100/12) = 7.071 A.
- [ai] 1 atom (solute only), 3 components: sqrt(3*100/12) = 5.000 A.
- [ai] all 4 atoms, 3 components: sqrt(12*100/12) = 10.000 A.
- [ai] FULL DECORRELATION (two chaotically-separated trajectories): the system is harmonically restrained with k=0.5 and kT=300K*BOLTZMANN_KCAL ~ 0.596 kcal/mol, so per-component sigma = sqrt(kT/k) = 1.09 A and two independent draws differ with sigma_diff = 1.54 A. Decorrelation saturation RMSD ~ 1.5 A. Even for uniform-in-box positions it would be only sqrt(100/6) = 4.08 A.
- [ai] OBSERVED = 8.975 A. That is 5.8x the decorrelation-saturation value and within 3.6% of the 8.660 A "whole water molecule displaced by one box in all three components" signature.
- [ai] TWO CONCLUSIONS, both new:
- [ai] (1) H-ILLPOSED is REFUTED as the explanation of the 8.975 A number. Chaotic/Langevin amplification of ULP noise in this harmonically-bounded 4-atom system cannot exceed ~1.5 A no matter how many steps run. (It survives only as a weaker claim about the fragility of a 1e-10 A gate in general, not as the cause here.) This also independently explains why 10 steps was enough: the divergence is a single discrete event, not an exponential ramp.
- [ai] (2) Stage 0a resolves to ONE-BOX JUMP, H1/wrap family CONFIRMED — not "AMBIGUOUS" as I estimated before computing the RMSD normalisation, and not "arbitrary". The spec's 0a discriminator table ("~17.95 -> half-box; ~8.975 -> one-box; >=30 -> arbitrary") was written for a hypothesised box of 17.95 A and compared the RMSD directly against a box length; with N=4 and an RMSD normalised over 12 scalars, the correct comparison value for a one-box jump of the water is box*sqrt(9/12) = 0.866*box = 8.66 A. The table's arithmetic was wrong; its conclusion (one-box jump) is right for the wrong reason.
- [ai] G1-REPLACEMENT CANDIDATES (the original tie-point G1 is void; what replaces it):
- [ai] G1'-TRANSLATION (preferred): re-run the identical test with every position rigidly translated by an offset that puts no atom on or near a wrap discontinuity — e.g. O at [3.1,3.3,3.7], H1/H2 translated identically, solute at [7.2,6.9,7.4] — same seed, same box, same dt, same gamma, same step count. Rigid translation of a periodic system is a physical symmetry, so the dynamics are equivalent; only the distance from each atom to a wrap discontinuity changes. PREDICTS: if H-DISCONTINUITY holds, max RMSD collapses from 8.975 A to ~1e-15 A. If it persists at ~8.9 A, H-DISCONTINUITY is refuted and the wrap-family story dies with it. Hand-constructible, deterministic, one edit, no new diagnostic script, and it discriminates the ONE mechanism that survives derivation.
- [ai] G1''-BITEXACT (cheap companion, not a substitute): at step 0, dump x = delta/box at all six sites for both the loop and the vmap path and compare BITWISE (jnp.frombuffer / .view(int64)). Three outcomes, all informative: bit-identical everywhere -> no rounding site can ever differ and the entire rounding investigation is closed; differing by 1 ULP at some site -> H-LOWER is live and the site is named; differing by >> ULP -> something upstream is already wrong. This is the honest replacement for "prove the hot regime is deterministic", because it tests the PRECONDITION for any rounding-site divergence rather than a regime we now believe is unreachable.
- [ai] G1'''-IDIOM-A/B (demoted to a control, not primary): PROLIX_PBC_ROUND_MODE=round vs floor, back-to-back. Still worth running because it costs one env var and it directly tests answer (A2) — that the L336 conversion never fixed anything. But it is NO LONGER primary evidence, because under H-DISCONTINUITY both idioms are predicted to fail identically.
- [ai] DROPPED: the original G1 (drive a site's delta/box onto an exact half-integer through the real call graph). Reason: derivation shows it requires box = 2*r_OH = 1.914 A at L790/793, or a ~5 A SETTLE correction at L188/L336/L1294/L1346. Neither is constructible from a physical configuration. Keeping it as a mandatory GREEN condition would make the acceptance criteria unsatisfiable — exactly the defect the spec's own REVERSAL B predicted.
- [critic] WHAT I COULD NOT VERIFY IN THIS SESSION (must not be laundered into the artifact as settled):
- [critic] U1. The call convention of shift_fn. settle.py invokes it as _langevin_step_a(positions, momentum, mass, dt, shift_fn) (L1265, L1322) and I could not locate _langevin_step_a's body in settle.py within this session's read budget. The test fixture defines _shift_fn(dR, box) = dR - box*jnp.round(dR/box). Whether its first argument receives an ABSOLUTE POSITION (making the discontinuity land at +/-box/2, i.e. exactly on the solute) or a DISPLACEMENT (a different story entirely) decides which atom sits on the discontinuity. Either way at least one atom does — O at [0,0,0] for a mod-style wrap, solute at [5,5,5] for a min-image-style one — so H-DISCONTINUITY survives both readings, but the NAMED ATOM and the predicted per-atom RMSD signature differ. This is a pure code read and must be the FIRST action, before any cluster time.
- [critic] U2. Whether the test fixture's own shift_fn is a SEVENTH rounding site that the 0c grep never covered, because 0c was scoped to src/prolix/physics/settle.py only. It is jnp.round applied to an unnormalised argument, in the traced path of both Stage 1 tests, and it was invisible to every prior enumeration. Note it lives in the TEST, so converting it would change the test rather than the library — which is a different kind of finding and needs the user's judgement, not a fixer's default.
- [critic] U3. Whether XLA lowers B=1 vmap and unbatched to bit-identical kernels on the Engaging GPU. Not checkable without the cluster. G1''-BITEXACT is designed to answer exactly this.
- [critic] U4. Whether the 8.975 A figure was ever produced by a recorded A/B run at all, or is a docstring number inherited from an unlogged observation. It appears in settle.py's _settle_water_batch docstring (L309-316) with no job ID, campaign ID, or bath run attached. Every downstream inference in v1, v2 and this session rests on it.
- [critic] COUNTER-ARGUMENTS TO MY OWN H-DISCONTINUITY, recorded so they are not lost: (a) if the discontinuity flip were the mechanism, it should also fire on CPU/local, since nothing about a face crossing is GPU-specific — unless CPU happens to compute the loop and vmap paths bit-identically, which is plausible but assumed. (b) 8.975 vs the predicted 8.660 is a 3.6% gap I have not accounted for; a mechanism that predicted it exactly would be stronger. (c) the O is at a CORNER, so a mod-style wrap flip would move it in all three components simultaneously only if all three cross at once, which is a coincidence unless all three components are symmetric — which, at [0,0,0] with an isotropic restraint, they are. That is suggestive, not proof.
- [user] converge — the option space is enumerated. The derivation refutes the prior REACHABLE verdicts at all four flagged sites, the RMSD normalisation refutes the decorrelation alternative, and a new mechanism (H-DISCONTINUITY, driven by a pathological fixture rather than by rounding at all) has emerged with a cheap hand-constructible discriminator to replace the void G1. Ready to rank and eliminate.

## Decision Log
- [ACCEPT] Stage 0d verdict: 0d-UNREACHABLE at ALL SIX box-normalised rounding sites in settle.py (L188, L336, L790, L793, L1294, L1346). REVERSES session fd78460a's REACHABLE verdict for L790/L793 and L1294/L1346.: Derived, not asserted. INTEGER-LATTICE INVARIANT: every argument to these sites has the form delta = s + k*box + eps with k integer and |eps| ~ 1e-15 A, because the only non-physical coordinate motions in the call graph (shift_fn/jnp.mod wraps, settle_positions' image_correction) add EXACT INTEGER multiples of box. Nothing adds a half box. Hence delta/box = k + s/box and a tie requires |s| = box/2 = 5.0 A. Per-site s: L188 SETTLE geometric correction to O, <~0.05 A; L336 one A-step displacement v*dt/2 at dt=0.5 fs, <~0.02 A; L790/793 the TIP3P O-H bond vector, PINNED by the constraint at 0.9572 A; L1294/1346 oxygen rows = the L188 min-image residual <~0.05 A, hydrogen rows = SETTLE correction <~0.1 A plus an exactly-integer image offset, and non-water rows identically 0. Margins are 50x to 250x. The prior session's error was treating the attainable range as a continuous interval that "includes 0.5"; the attainable SET is a union of narrow clusters around integers {k +/- 0.1}, and 0.5 falls in the GAP between clusters. Range-inclusion was necessary-but-not-sufficient and the set is not an interval. Five loopholes probed and all closed: NPT box rescaling needs a 25-33% linear box change in ONE step; the only exactly-solvable reachability condition is box = 2*r_OH = 1.914 A at L790/793, smaller than a water molecule; anisotropic box does not help (the invariant is per-component); solute rows are identically zero; a pre-detonated state makes the site a symptom not a cause. This also confirms and extends the user's own physical reasoning: a single independent-wrap crossing of a short rigid bond puts dH/box near an INTEGER, and their constructed straddling-water case returning max_abs_atom_position_delta = 0.0 identically under both idioms is the predicted result, not a null result.
- [REJECT] The original G1 (constructed adversarial case driving delta/box onto an exact half-integer through the real settle_langevin call graph): VOID and DROPPED, not waived. It requires a physically impossible configuration: box = 2*r_OH = 1.914 A for L790/793, or a ~5 A SETTLE geometric correction for L188/L336/L1294/L1346. Retaining it as a mandatory GREEN condition makes the acceptance criteria unsatisfiable — precisely the defect the spec's own REVERSAL B predicted and branch (d) exists to absorb. This also retroactively vindicates the user's rejection of the "engineer a bit-exact G1 case" option on quantitative grounds rather than taste: the engineering required is an unphysical box, and a case built that way would prove nothing about the real regime.
- [DEFER] REVERSAL A(1) — the XLA-lowering / ULP-anywhere counter-hypothesis (H-LOWER): Partly CORRECTED, partly UNRESOLVED, and it must not become the new default story just because H-TIE fell. CORRECTED: the counter-hypothesis as the spec states it ("the two forms can disagree by an ULP in x itself, ANYWHERE") is not quite right. round(x) and floor(x+0.5) are BOTH pure, exactly-specified functions of the float64 bits of x — neither has any FMA sensitivity of its own. If x is bit-identical on both paths, both idioms agree; if x differs by 1 ULP, both idioms disagree only when that ULP straddles a tie, with the SAME ~1-ULP measure. Neither idiom is more robust to x differing. Two consequences: (i) settle.py L310-316's claim that floor(x+0.5) is "deterministic round-half-up regardless of FMA reassociation order" is FALSE AS WRITTEN and self-refuting — it names banker's rounding while describing an FMA-reassociation mechanism that banker's rounding is orthogonal to; (ii) floor(x+0.5) is in fact slightly LESS accurate than round(x), inheriting a double-rounding error (worked case: x = 0.5 - 2^-54 gives x+0.5 = 1 - 2^-54, exactly halfway between representable doubles, rounds to 1.0, so floor gives 1 while round(x) gives 0 — they disagree one ULP BELOW the tie, not only at it). This also corrects the user's own empirical sweep, whose 2e-6 step size was ~1e10 ULPs and could not resolve the sub-ULP structure. The narrow-band conclusion survives; the "only at exactly 0.5" phrasing does not. UNRESOLVED: what DOES survive of REVERSAL A(1) is the weaker and still-live claim that adding an op changes XLA's FUSION boundaries for the surrounding expression, so x = delta/box is COMPUTED differently. That cannot be confirmed or refuted without inspecting lowered HLO/PTX on the target GPU, which this session cannot do. Evidence that would distinguish it from the tie story: a bitwise dump of x at each site on both paths at step 0 (differing by exactly 1 ULP implicates lowering; bit-identical closes the rounding investigation entirely), plus the control that a semantically-null perturbation such as round(delta/box + 0.0) "fixes" the divergence just as well — which would prove the fix is fusion-incidental and therefore un-warranted and version-fragile.
- [MERGE] The landed hardening (commits 8867216, 091a83a) and its causal story: KEEP THE CODE, REWRITE THE STORY. The conversions stay: a single project-wide PBC rounding idiom selected by one module-level constant is good hygiene, it makes the A/B control possible at zero further cost, and reverting would churn the two most delicate blocks in the file for no gain. But three claims attached to it must be corrected, because they are now known to be unsupported or wrong: (1) settle.py L310-316's assertion that floor(x+0.5) is FMA-reassociation-deterministic is false — both idioms are pure functions of x; (2) the docstring's attribution of the 8.975 A divergence to banker's rounding is an unverified causal claim carrying no job ID, campaign ID or bath run, and the tie point is now shown unreachable at that very site by the widest margin of all six (s <~ 0.02 A against the 5.0 A required); (3) any framing of floor(x+0.5) as "more correct" is backwards — it is marginally less accurate and is adopted as a CONVENTION, not an accuracy improvement. Required edits: rewrite L309-316 and L330-331 to state the convention and explicitly disclaim the causal attribution; delete or rewrite L161's stale sentence advocating round() (the spec already flagged this); annotate the 8.975 A figure as a historical, unlogged observation pending re-measurement. The commit that carries these edits must not claim the fix is causal.
- [ACCEPT] Backlog #4166 (weak-friction dt cap follow-up) and the P5 dt=1.0 fs / size-sweep conclusions: UNAFFECTED — no action, and this should be stated explicitly so nobody re-opens it on the strength of this finding. #4166 concerns thermostat-to-constrained-DOF coupling at weak friction, a physics mechanism with no dependency on PBC rounding tie-breaking. The P5 gates (job 19774893, campaigns ba334c1f / 46a4d737) are STATISTICAL observables (T_rot, T_total means with quoted sigma), not bitwise loop-vs-vmap comparisons, so they are insensitive to everything in this session. One narrow caveat worth recording rather than acting on: if the Stage 1 bit-exactness probe shows that B=1 vmap and unbatched do NOT lower to bit-identical kernels on the Engaging GPU, then no future validation anywhere in the project should assert bitwise batched-vs-unbatched equality as a gate — only statistical agreement. That is a project-wide testing-convention note, not a defect in any landed P5 result.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** Six months on, this has definitively failed. Most likely stories, in order.

FAILURE 1 (most likely) — THE GREEN WAS OVER-READ. G1'-TRANSLATION came back green: the translated fixture passed at 1e-15 A. Someone edited small_water_system to move the atoms off [0,0,0] and [5,5,5], the test went green on Engaging, #934 was closed as "fixture pathology", and the causal claim in settle.py's docstring was never corrected. Twelve months later a jaxlib bump reintroduces a loop-vs-vmap ULP difference somewhere with no pathological fixture to blame, and THIS ARTIFACT's 0d table gets cited as proof that rounding was ruled out — a correct table used to license a conclusion it does not support. This is the runner-up's steelman coming true exactly as written. MITIGATION THE ARTIFACT MUST CARRY: a green G1'-TRANSLATION is necessary but NOT sufficient to close #934. G1''-BITEXACT must be reported alongside it, and if it shows a 1-ULP loop-vs-vmap difference at any site, the follow-up item stays open regardless of how green the translated fixture is. Moving a fixture off a discontinuity hides a divergence; it does not prove the library is deterministic.

FAILURE 2 — S0-FIRST WAS SKIPPED. Nobody read _langevin_step_a. The shift_fn convention (U1) stayed unresolved, so the artifact's claim about WHICH atom sits on the discontinuity was never pinned down, and the translation offsets in G1'-TRANSLATION were chosen against the wrong convention — moving the water off a corner that was never the discontinuity while leaving the solute sitting on one. G1'-TRANSLATION came back RED, H-DISCONTINUITY was declared refuted, and the whole line of inquiry was abandoned on a badly-constructed test. MITIGATION: S0-FIRST is BLOCKING and is a five-minute code read. The artifact must state that a RED G1'-TRANSLATION is only interpretable if S0-FIRST was recorded first, and that the translation offset must move EVERY atom away from BOTH candidate discontinuity sets (0 and +/-box/2), which the suggested offsets do — but only by luck if the convention is unknown.

FAILURE 3 — THE 3.6% GAP WAS THE REAL SIGNAL. 8.660 predicted, 8.975 observed. I flagged it and moved on. It turns out the extra 3.6% was a second, smaller, independent effect riding on top of the one-box jump, and chasing the one-box jump alone fixed nothing. MITIGATION: the first-divergence dump must report the PER-ATOM, PER-COMPONENT difference vector, not just the scalar RMSD. If exactly 9 of 12 components differ by ~10.36 A and 3 by ~0, that is one story; if the 12 components are ragged, that is another, and the RMSD scalar cannot distinguish them. This costs nothing and would have caught it.

FAILURE 4 — A3 WAS NEVER FOLLOWED UP. I identified that the tie IS genuinely reachable for minimum-image applied to PAIR SEPARATIONS (density at box/2 is smooth and non-zero for inter-atomic distances, unlike every intramolecular site in settle.py), named prolix.physics.pbc and the jax_md space functions as where to look, and then made it item 5 of 7 in a ranking. Nobody looked. The real rounding bug was in the neighbor list or the PME real-space cutoff all along, in the 64-water test that we de-prioritised in favour of the 4-atom one. MITIGATION: A3 must be filed as its own backlog item with the 0c grep explicitly RE-SCOPED beyond settle.py, not left as a paragraph. It is the only place in this codebase where the tie-point mechanism is not ruled out by construction, and that fact is the most transferable thing in this session.

FAILURE 5 (lower probability) — THE DERIVATION WAS RIGHT AND IRRELEVANT. Everything in the 0d table holds, the tie is unreachable, and the actual cause was H2 (padding/scan desynchronisation) or H3 (eigh) all along — hypotheses this session did not touch because Stage 0b and 0a-instrumentation were out of scope. Six months of rounding forensics on a non-rounding bug. MITIGATION: the artifact must state plainly that it re-derives 0d ONLY, that Stage 0b's H2 determination is still outstanding and unaffected by anything here, and that a green on all three revised Stage 1 arms does NOT clear H2 or H3.

None of these overturn the winner — the 0d table is a derivation and stands independently of which mechanism turns out to be responsible. Failures 1 and 4 are the serious ones and must appear as explicit constraints in the handoff, not as afterthoughts.
**AI:** _not recorded_

## Acceptance Criteria
**Given** FIXED (cannot change):
- The landed hardening commits 8867216 / 091a83a stay. floor(x+0.5) as the project-wide PBC idiom is not up for reversal in this session.
- settle.py's physics semantics: minimum-image at each of the 6 sites must keep doing what it does; we are only reasoning about which rounding tie-break fires and when.
- The bug's definition: "passes locally (WSL2/CPU), diverges on Engaging (GPU)". Any evidence path that only produces a local green proves nothing.
- Float64 (x64 enabled in the target test). ULP at |x|~1 is 2^-53 ≈ 1.1e-16.
- shift_fn / jnp.mod only ever add INTEGER multiples of box to a coordinate. This is the load-bearing invariant of the whole re-derivation and must be stated, not assumed.
- Cluster budget: at most one job. Backlog #4193 hang risk applies.

NEGOTIABLE:
- Stage 0d's verdict itself (that's the point of this session).
- G1's definition, or its existence. The spec's own REVERSAL B already anticipated that G1 could become unsatisfiable; I am willing to drop it outright and say so.
- Which test/scale Stage 1 runs at, and how long. The 20k-step 64-water run is not sacred.
- The metric. max_abs_atom_position_delta per step is probably right but the GATE built on it may not be.
- Whether the causal story in settle.py's own docstring (L309-316: "floor(x+0.5) gives deterministic round-half-up regardless of FMA reassociation order ... producing an 8.975 Å RMSD") survives. I suspect this comment is itself an unverified claim and possibly numerically wrong as stated.
- Whether TOLERANCE-DOC deserves partial rehabilitation — not as "loosen and move on", but as a testable claim about what a B=1-vmap-vs-unbatched comparison can possibly guarantee on GPU.

Stress-test assumption I most want attacked: that "delta = (bounded small physical quantity) + (exact integer multiple of box)" holds at ALL five sites, making delta/box = integer + small/box and therefore never within 1 ULP of a half-integer unless small ≈ box/2 = 5 Å. If that invariant has a hole, the whole UNREACHABLE verdict collapses.
**When** implementing 0d-UNREACHABLE AT ALL SIX SITES; BRANCH (d) FIRES; STAGE 1 IS RE-SPECIFIED AS A DISCONTINUITY-PROXIMITY TEST, NOT A ROUNDING-IDIOM A/B.

=== 1. CORRECTED STAGE 0d TABLE (replaces session fd78460a's) ===
Invariant (derive it, do not inherit it): every argument to a box-normalised rounding site in settle.py has the form delta = s + k*box + eps, k integer, |eps| ~ 1e-15 A, because the only non-physical coordinate motions in the call graph — shift_fn / jnp.mod wraps and settle_positions' image_correction — add EXACT INTEGER multiples of box. Nothing adds a half box. So delta/box = k + s/box, and round()-vs-floor(x+0.5) can only disagree if |s| = box/2 = 5.0 A (box = [10,10,10] from test_settle_batched.py L72).
  L188  settle_positions delta_o        s = SETTLE geometric correction to O, <~0.05 A   ->  s/box <~ 0.005   UNREACHABLE (100x margin)
  L336  _settle_water_batch unwrap      s = one A-step displacement v*dt/2 at dt=0.5 fs, <~0.02 A  ->  <~0.002  UNREACHABLE (250x margin; ALREADY CONVERTED, and the LEAST reachable of the six)
  L790  dH1 = r_H1_unc - r_O_unc        s = TIP3P O-H bond vector, PINNED by the constraint at 0.9572 A  ->  0.0957  UNREACHABLE (5.2x margin; tie requires box = 2*r_OH = 1.914 A)
  L793  dH2                             identical to L790                                              UNREACHABLE
  L1294 dx_1 = x_con_1 - x_unc_1        O rows: the L188 min-image residual <~0.05 A. H rows: SETTLE correction <~0.1 A PLUS an exactly-integer image offset. Non-water rows: identically 0.  UNREACHABLE
  L1346 dx_2                            identical to L1294                                            UNREACHABLE
OVERALL: 0d-UNREACHABLE. Prior REACHABLE verdicts for L790/793 and L1294/1346 are REVERSED. The prior error was treating the attainable range as a continuous interval that "includes 0.5"; the attainable SET is a union of narrow clusters around integers {k +/- 0.1}, and 0.5 lies in the GAP between clusters. L1294/1346 is NOT structurally different from L790/793 in the way that matters — "same atom computed two ways" still differs only by (bounded SETTLE correction) + (integer image offset), because image_correction is by construction an integer multiple of box.
Loopholes probed and closed: NPT box rescaling (needs 25-33% linear box change in ONE step; both Stage 1 targets are NVT anyway); unphysically small box (the only exactly-solvable condition, box = 1.914 A); anisotropic box (invariant is per-component); solute rows (identically zero); pre-detonated state (makes the site a symptom, not a cause).

=== 2. THE MECHANISM THAT SURVIVES: H-DISCONTINUITY ===
The spec conflated two different points. For f(x) = x - box*ROUND(x/box) the output jumps by a full box wherever x/box crosses a half-integer, and round() and floor(x+0.5) place that jump at the SAME location — they differ only in which side the exact boundary point falls on. So:
  - "Do the two IDIOMS disagree?" is the tie question: measure ~1 ULP, UNREACHABLE everywhere in settle.py.
  - "Do the loop and vmap PATHS disagree?" is the discontinuity question: it fires when x/box lands within 1 ULP of a half-integer AND the two paths' x differ by >= 1 ULP. It requires NO rounding-mode difference and is NOT fixed by converting the idiom.
FIXTURE PATHOLOGY: small_water_system puts the oxygen at EXACTLY [0,0,0] (a box corner) and the solute at EXACTLY [5,5,5] = box/2 in every component, with box = [10,10,10]. 5.0/10.0 = 0.5 exactly; 0.0/10.0 = 0.0 exactly. Whichever wrap convention applies, one of those atoms sits precisely ON a wrap discontinuity — and BOTH are harmonically restrained to those exact points (k = 0.5 kcal/mol/A^2, positions_ref identical to the initial coordinates), so their probability density at the discontinuity is a Gaussian CENTRED on it (sigma = sqrt(kT/k) ~ 1.1 A). Density at the discontinuity is MAXIMAL. That is the exact inverse of every settle.py site, where it is identically zero.
MAGNITUDE CHECK (new; the prior sessions could not do this): compute_rmsd (test L250-252) averages over ALL 12 scalars, not per-atom distances. One box (10 A) applied to the 3 water atoms in all 3 components gives sqrt(9*100/12) = 8.660 A. Observed: 8.975 A (3.6% high, residual NOT accounted for — flagged). By contrast full decorrelation of this harmonically-bounded system saturates at ~1.5 A, and even uniform-in-box positions give only 4.08 A. So 8.975 A is BOX-QUANTISED, not chaotic amplification. Stage 0a therefore resolves to ONE-BOX JUMP / H1-wrap-family CONFIRMED — the spec's 0a table reached that conclusion by wrong arithmetic (it compared an RMSD normalised over 12 scalars directly against a box length, and assumed box = 17.95).
Honest limitation: H-DISCONTINUITY is an AMPLIFIER hypothesis, not a SOURCE hypothesis. It explains the magnitude and the fixture; it does not explain why the two paths differ by an ULP in the first place, nor why the test passes locally (which requires assuming CPU emits bit-identical loop/vmap code and GPU does not).

=== 3. REVISED STAGE 1 — three arms, one job, no tie-point construction ===
G1 as originally conceived is DROPPED (void, unsatisfiable). Replacements, in priority order:
  (S0-FIRST, zero compute, BLOCKING) Read _langevin_step_a's body and record how shift_fn is invoked. The fixture's _shift_fn(dR, box) = dR - box*jnp.round(dR/box) is a SEVENTH rounding site that no prior enumeration covered because 0c was scoped to settle.py. Whether its first argument receives an absolute position (discontinuity at +/-box/2 = the solute) or a displacement decides which atom is on the discontinuity. Do this before spending any cluster time.
  (G1'-TRANSLATION, PRIMARY) Re-run the identical test with every position rigidly translated so no atom sits on or near a wrap discontinuity — e.g. O at [3.1,3.3,3.7], H1/H2 translated identically, solute at [7.2,6.9,7.4] — same seed, box, dt, gamma, step count. Rigid translation of a periodic system is a physical symmetry, so the dynamics are equivalent and only discontinuity-proximity changes. PREDICTS: RMSD collapses from ~8.975 A to ~1e-15 A if H-DISCONTINUITY holds; persists at ~8.9 A if it does not, killing the wrap-family story outright. Hand-constructible, deterministic, one fixture edit.
  (G1''-BITEXACT, PRIMARY) At step 0, dump x = delta/box at all six sites on both paths and compare BITWISE (.view(int64)). Bit-identical everywhere -> no rounding site can ever differ and the entire rounding investigation closes. Differing by 1 ULP -> H-LOWER is live and the site is NAMED. Differing by >> ULP -> something upstream is already wrong. This tests the PRECONDITION for any rounding divergence, which is the honest replacement for "prove the hot regime is deterministic".
  (G1'''-IDIOM-A/B, demoted to CONTROL) PROLIX_PBC_ROUND_MODE=round vs floor back-to-back, import-time selection, same node/seed/SHA. Costs one env var. Directly tests whether the L336 conversion ever fixed anything. NOT primary: under H-DISCONTINUITY both idioms are predicted to fail identically.
  (G2, retained) N >= 5 seeds under TOL. Unchanged and still mandatory as evidence that no other regime is hot.
SCALE: run the SMALL target (test_settle_batched.py::test_settle_batched_vs_unbatched, 4 atoms, 10 steps) as the primary vehicle. The divergence is a single discrete event, not an exponential ramp, so the 20k-step 64-water run is NOT required to observe it and should not be the first spend. This also sidesteps the #4193 hang budget almost entirely.
EVIDENCE RULE: RED stays conclusive on a single observation. GREEN now requires G1'-TRANSLATION + G1''-BITEXACT + G2. G1'''-IDIOM-A/B is reported, never gating.

=== 4. BRANCH SELECTION ===
Branch (d) FIRES — but with a correction to how the spec frames it. The spec expected branch (d) to mean "the tie-point mechanism is falsified, re-scope to H2/H3." The actual finding is stronger and more specific: the tie-point mechanism is falsified AND a concrete replacement mechanism (H-DISCONTINUITY, rooted in a pathological test fixture rather than in library code) is identified, with a cheap constructible discriminator. The five conversions stay landed as consistency-only hardening on a NON-CLOSING commit; #934 stays OPEN; the positive-check gate applies in full — a new backlog item must be FILED with a recorded ID carrying this 0d table, the corrected 0a arithmetic, and the S0-FIRST shift_fn finding, and #934's re-scope note must link to it. UNREACHABLE is not a tidy ending and must not be written up as one.
**Then**
  - [ ] _add specific measurable criteria_
