---
session_id: fd78460a
topic: Decide the fix approach for prolix backlog #934 (P5-C) batched SETTLE vmap divergence — REVISION 2, superseding session 33ef1b3b
task_type: diagnostic
winner: STAGED-BISECT v2 — REPLACEMENT ACCEPTANCE-CRITERIA BLOCK for backlog #934 (P5-C).

SUPERSESSION NOTICE: this block SUPERSEDES contemplex session 33ef1b3b and its artifact .praxia/docs/specs/260812_decide-the-fix-approach-for-prolix-backl.md ("v1"). v1 is locked in Phase 4 and could not be amended in place. Where this block and v1 disagree, THIS BLOCK GOVERNS. Do not execute v1's acceptance criteria: they end in an unfilled template placeholder and contain two mutually contradictory GREEN pass-criteria. This block is standalone — a fixer needs nothing from v1.

APPROACH (settled in round 0 by independent challenger and defender review; NOT reopened): STAGED-BISECT — zero-compute discriminators first, then ONE instrumented A/B cluster job, with an asymmetric red/green evidence rule. TOLERANCE-DOC stays REJECTED. H1-ELIMINATE (deleting settle_positions' second re-wrap) stays DEFERRED. Cluster re-validation stays MANDATORY. The rounding conversions land as HARDENING, never as a confirmed cause.

WHAT THIS REVISION CHANGED, AND WHY:
(A) Fix scope was wrong. settle.py has SIX box-normalised rounding sites, not the two v1 discusses; four previously-undiscussed ones sit in the identical traced path as both Stage 1 tests.
(B) The hot/cold arithmetic was mis-stated on both sides, which produces a new blocking zero-compute gate (Stage 0d) and a fourth branch.
(C) v1's "SAME process" A/B framing directly contradicts its own "no traced predicate" hard constraint; it is relaxed.

=== VERIFIED SITE ENUMERATION (read directly from src/prolix/physics/settle.py during this revision) ===
  L171   settle_positions                     -box * jnp.round(delta_o / box)   delta_o = pos_O_c - pos_O_new (sub-Angstrom SETTLE geometric correction). THE ONLY SITE v1 TARGETS.
  L319   _settle_water_batch                  delta - box * jnp.floor(delta/box + 0.5)   ALREADY CONVERTED
  L773   _r_step_conserve_angular_momentum    dH1 - box * jnp.round(dH1 / box)   dH1 = H1 - O, atoms independently wrapped by shift_fn
  L776   _r_step_conserve_angular_momentum    dH2 - box * jnp.round(dH2 / box)
  L1277  settle_langevin R1-step              dx_1 - box * jnp.round(dx_1 / box)   dx_1 = x_con_1 - x_unc_1
  L1329  settle_langevin R2-step              dx_2 - box * jnp.round(dx_2 / box)
L773/776/1277/1329 are invoked UNCONDITIONALLY by settle_langevin(...).apply_fn whenever water_indices is not None — true of BOTH Stage 1 target tests — so they are INSIDE the failing test's blast radius. v1 rejected DETERMINISM-SWEEP's blanket conversion specifically because it would touch sites OUTSIDE that radius; that reasoning does not shield these four, it argues for converting them. Their own inline comments document the hazard: L737-741 "water molecules near a periodic boundary may have O at x~0 and H at x~L_box ... catastrophically large impulse -> NaN"; L1268-1274 "the raw difference can jump by a full box vector ... a spurious ~box-sized impulse that detonates the integrator at liquid density".

=== STAGE 0 — ZERO COMPUTE, ALL FOUR BLOCKING PREREQUISITES ===
(0a) ARITHMETIC DISCRIMINATOR. Read the box vector from tests/physics/test_settle_batched.py::test_settle_batched_vs_unbatched (line 449) and from tests/physics/test_settle_64water_batched_10ps.py. NOTE: v1 wrote "tests/physics/test_settle_batched_vs_unbatched" as though it were a file path; it is a FUNCTION inside test_settle_batched.py. Compare 8.975 A against the box: ~17.95 -> half-box, minimum-image SIGN disagreement (H1 family); ~8.975 -> clean one-box jump (H1 family); >=30 -> arbitrary, wrap story WEAK, re-weight toward H3/eigh. An AMBIGUOUS result is itself informative and re-weights toward H3 — it must be recorded, not shrugged off.

(0b) H2 EXCLUSION — CRITERION CORRECTED. v1's criterion ("gamma=0 OR keys split before the scan") does not test what H2 hypothesises. H2 says PADDED rows advance lax.scan a different number of times than unpadded rows, desynchronising per-step noise draws. The discriminator is PADDING, not gamma. H2 is structurally excluded IFF BOTH hold:
   (i) the bundle is HOMOGENEOUS — water_mask is None or all-True, and every replica has identical n_atoms / n_waters — so no padded row exists that could advance the scan differently; AND
   (ii) INSIDE the scan BODY, each replica's per-step key derives purely from its own carry leaf (e.g. key, subkey = jax.random.split(carry.key) on a per-replica vmapped carry), with NO dependence on water_mask, on any padded-row quantity, or on another replica's data.
Record (i) and (ii) explicitly for each test. Checking only what happens BEFORE the scan is insufficient. Input to VERIFY, not inherit: test_settle_64water_batched_10ps.py reportedly runs gamma=10 (NONZERO) with NO padding and no water_mask (uniform batch, both replicas the identical system) — if so it is excluded by (i), and would NOT have been excluded by v1's gamma clause. Apply the same two-part check to test_settle_batched_vs_unbatched (line 449).
If (i) fails for either test, H2 stays LIVE and gets a gamma=0 control arm in Stage 1: divergence vanishes -> H2 implicated; persists -> H2 excluded.

(0c) SCOPE GREP. rg 'jnp\.round\(|jnp\.mod\(' src/prolix/physics/settle.py. Classify every hit twice: box-normalised or not, and inside the traced apply_fn under test or not. The six-site table above is the expected result — reconcile any discrepancy before proceeding. settle_velocities (L850) takes no box parameter and is structurally exempt.

(0d) REACHABILITY ARITHMETIC — NEW, BLOCKING, and the cheapest available falsifier. jnp.round and floor(x+0.5) can only disagree by tie-breaking rule when x = delta/box lies within ULP of a HALF-INTEGER (|delta| = box/2, 3*box/2, ...). For each of the five unconverted sites, DERIVE the physically attainable range of |delta|, divide by the 0a box, and state whether any half-integer falls inside it.
   PRELIMINARY HYPOTHESIS — TO BE CONFIRMED OR REFUTED BY THE FIXER, NOT ASSUMED TRUE: none of the five appears to reach a half-integer. L171 gives x ~ 0.01 (sub-Angstrom SETTLE correction over a >=10 A box); L1277/L1329 give x ~ 0 (no wrap) or x ~ 1.0 (wrap); L773/L776 give x ~ 0.05 (unwrapped O-H bond) or x ~ 0.95 (wrapped). THE L1277/L1329 ENTRY IS THE WEAKEST AND MUST BE DERIVED, NOT INHERITED: dx = x_con - x_unc is a difference of two INDEPENDENTLY-wrapped quantities (shift_fn wraps x_unc; settle_positions reconstructs x_con in the unwrapped frame of positions_old), so its attainable range may genuinely cover box/2.
   This hypothesis also CORRECTS v1: v1's REVERSAL 2 wrote "delta ~= box, so delta/box ~= +/-0.5" to justify calling L319 hot. That is wrong arithmetic — delta ~= box gives x ~= 1.0, not 0.5. So v1's stated reason for L319 being hot does not hold as written, even though converting L319 empirically fixed an observed cluster divergence. That tension is real and is carried into branch (d), not resolved here.
   COUNTER-HYPOTHESIS the fixer must also weigh: the two forms may differ NOT by tie-breaking rule but because they lower to different XLA instruction sequences — the extra addition in floor(x+0.5) changes where the value is materialised versus fused, so the two can disagree by an ULP in x itself, ANYWHERE, not only at half-integers. Under that reading every range argument in both plan versions is beside the point. This reading is arguably MORE consistent with the L319 empirical result than the tie-point story is.
   PRE-REGISTERED OUTCOMES:
     0d-REACHABLE — a half-integer lies within some in-path site's derived range. That site is the target of the mandatory constructed adversarial case (G1). Proceed to Stage 1.
     0d-UNREACHABLE — no half-integer at any in-path site. BRANCH (d) fires. Do NOT submit the Stage 1 cluster job as specified.

=== CONSTRUCTED ADVERSARIAL CASE (G1) — RECIPE ===
Applicable only under 0d-REACHABLE. Build it at the site 0d identifies, through the REAL settle_langevin(...).apply_fn call graph — not by calling an internal helper directly, which would not exercise the traced path under test.
  If the site is L773/L776 (dH = H - O): construct a water straddling a periodic face — oxygen placed just inside the +x face, both hydrogens placed so that shift_fn's independent per-atom wrap lands them across it — and tune the O-to-face offset so that |dH_x| / L_x lands on the half-integer 0d identified. The box-to-bond-length ratio is the free parameter: the required |dH| is box/2, so the construction is feasible only for the box sizes 0d certifies.
  If the site is L1277/L1329 (dx = x_con - x_unc): drive the water across the face on the step SETTLE acts, choosing dt and the initial velocity along x so that the A-step displacement plus the SETTLE correction places |dx_x| at box/2. Report dt and the velocity used.
  If the site is L171 (delta_o): NOT a plausible target. v1's own REVERSAL 2 argues delta_o is a sub-Angstrom geometric correction, so reaching the tie point there requires the constrained position to have already moved ~half a box, meaning something upstream failed first. v1's language implying L171 is the adversarial target is WITHDRAWN. If 0d nonetheless certifies L171 as reachable, that finding itself supersedes REVERSAL 2 and must be recorded as such.
  The constructed case runs deterministically (fixed seed, gamma may be set to 0 for it) and is asserted under the same TOL as the seeded cases.

=== THE FIX ===
Convert to the deterministic idiom, mirroring the already-converted L319: replace X - box * jnp.round(X / box) with X - box * jnp.floor(X / box + 0.5).
  MANDATORY at L171, L773, L776, L1277, L1329 — unless an individual site is affirmatively argued COLD in the work order using explicit delta-magnitude reasoning and the 0d table. A default defer is NOT an acceptable exclusion for L773/776/1277/1329: they are PRESUMPTIVELY HOT on the strength of the code's own adjacent comments, and excluding any of them requires a written argument.
  Also remove/replace the stale sentence at L161 — "The only exception is a genuine PBC crossing (|delta| ~ box), handled by round()" — which argues FOR round() in precisely the regime under investigation.
  LAND AS ONE REVIEWABLE CHANGE with the dt=1.0 fs cap-lift gate (reference job 19774893) as the regression guard. L773/776 sit inside _r_step_conserve_angular_momentum, the code the P5 C3 work (678c9cb) added to stop T_rot drifting; L1277/1329 sit in the R-step momentum correction. These are the two most delicate blocks in the file, not a cosmetic sweep. PRE-DECLARED RESPONSE TO A REGRESSION: split the commit per site and bisect — do NOT proceed with the combined change.
  The commit message must state the mechanism per the 0d result and must NOT assert the fix is the confirmed cause.

=== STAGE 1 — ONE CLUSTER JOB (only under 0d-REACHABLE) ===
Extend scripts/experiments/p5_settle_batch_divergence.py; do not write a new diagnostic.

A/B CODE SHAPE — resolves v1's contradiction between "no traced predicate" and "SAME process". REQUIRED: select the rounding idiom at MODULE IMPORT TIME via a module-level constant in settle.py (e.g. _PBC_ROUND, bound once at import scope from an environment variable), so each traced program contains exactly ONE fused code path with no branch. Arm A (round) and arm B (floor) are two back-to-back SUBPROCESS invocations from the SAME slurm job, SAME node, SAME allocation, SAME seed, SAME box, SAME git SHA, differing only in that environment variable.
  FORBIDDEN: any bool or array flag threaded into a traced function; any lax.cond or Python branch inside the traced body; any environment-variable read inside the traced body; any change to settle_positions' signature.
  v1's "SAME process" wording is RELAXED to "same job, same node, back-to-back". It contradicted v1's own pre-mortem FAILURE-1 mitigation, which names import-time selection or separate process invocations as the safe mechanism. Everything "same process" was buying — one machine, one seed, no cross-run confound — is preserved.
  If even import-time selection is judged to perturb lowering, FALL BACK to REPRO-FIRST (run both target tests unmodified at HEAD) and accept the extra queue round-trip.

METRIC — single, named, closable: max_abs_atom_position_delta = max over atoms i and Cartesian components c of |x_loop[i,c] - x_vmap[i,c]|, in Angstrom, evaluated per step. FIRST-DIVERGENCE STEP = the smallest step index at which this exceeds TOL. Terminal-trajectory RMSD is explicitly NOT the gate metric — it measures the amplified cascade, not the primitive error. At first divergence, dump: offending molecule index; its pre/post O/H1/H2 positions on both paths; the value of delta/box at EVERY one of the five sites for that molecule; and whether the primitive |delta| is near an integer or half-integer multiple of box.

TOL = 1e-10 Angstrom, applied to max_abs_atom_position_delta. Justification: the frame requires loop/vmap equivalence; a wrap-class error is box-quantised (order 1-20 A) while legitimate float64 reassociation noise at Angstrom-scale coordinates is order 1e-15 A per operation, accumulating to at most ~1e-12 A over the trajectory lengths in play. 1e-10 A therefore sits several orders above achievable round-off and ten-plus orders below any wrap error, so no result is ambiguous and no "small but real" divergence can be waved through.

SIDECAR — the EXISTING scripts/experiments/p5_settle_batch_divergence.bth.toml is SUPERSEDED and must be REWRITTEN as part of this item; it cannot be reused. Three reasons, all verified by reading it: (1) it gates on max_diff_with_box < 1e-5, a threshold chosen for a different question ("is the CPU float64 box unwrap stable?") and five orders too loose to detect slow accumulation; (2) its hypothesis string still cites "settle.py:237" for the _settle_water_batch site, now L319 — stale; (3) its pass-branch decision text reads "platform_precision root cause confirmed", a conclusion the run it gates cannot establish — exactly the post-hoc reading the asymmetric evidence rule exists to prevent. The rewrite keeps the [experiment] schema with exactly one outcome carrying is_residual = true, and encodes TOL = 1e-10 on max_abs_atom_position_delta plus the branch conditions below. Run under bth run inside a slurm wrapper sourcing scripts/slurm/_bth_env.sh; bth campaign create BEFORE the first run; pass --campaign to every run.

=== EVIDENCE RULE — THE ONE AUTHORITATIVE STATEMENT (overrides every other phrasing, in v1 or elsewhere) ===
RED is conclusive on a SINGLE observation. Any arm exceeding TOL on any case is red.
GREEN requires BOTH of the following. Each is necessary; neither alone suffices:
   (G1) the CONSTRUCTED adversarial case passes under TOL — MANDATORY WHENEVER 0d RETURNS REACHABLE, and it is the primary evidence, because it proves the identified hot regime is deterministic. If 0d returns UNREACHABLE, G1 is VOID (not waived) and branch (d) applies instead of the green/red branches.
   (G2) N >= 5 random seeds all pass under TOL — ALSO MANDATORY, because it provides evidence that no OTHER regime is hot.
Failure of G1 alone is conclusive red. Failure of G2 alone is conclusive red. Both run inside the same job, so requiring both costs nearly nothing.
This paragraph REPLACES v1's Evidence Rule section (which required N>=5 seeds and never mentioned a constructed case) AND v1's Decision-Log ACCEPT note (which said the constructed case was primary and multi-seed merely supplementary). Those two were contradictory; neither survives.

=== BRANCH ACTIONS — pre-declared, exhaustive, exactly one executes ===
(a) Post-fix arm GREEN (G1 and G2) AND pre-fix arm RED -> the fix is sufficient and CAUSAL. Land it, add a cluster-marked regression test, close #934 — SUBJECT TO THE CLOSURE SUB-RULE.
    CLOSURE SUB-RULE (blocking): #934 may close only if EVERY box-normalised round()/mod() site enumerated at 0c that lies within the traced apply_fn under test is either (i) converted alongside L171, or (ii) affirmatively argued COLD in the work order using explicit delta-magnitude reasoning and the 0d table. L773, L776, L1277 and L1329 are specifically flagged PRESUMPTIVELY HOT: excluding any of them requires a written argument, not a default defer.
    ADDITIONAL BLOCK ON THE CAUSAL CLAIM: do not write "banker's rounding was the cause" unless the first-divergence dump actually shows a box-quantised primitive error. If the primitive error is arbitrary-magnitude, the fix may have worked for a different reason and the closing note must say so.
(b) BOTH arms RED -> the fix is necessary-but-insufficient. Land the hardening as a NON-CLOSING commit. #934 stays OPEN and re-scoped. Read the first-divergence dump: box-quantised primitive error -> an unfound wrap site (feed the 0c list); arbitrary magnitude -> H3/eigh.
    POSITIVE-CHECK GATE (blocking; closes pre-mortem FAILURE 2): branch (b) is discharged only when a NEW backlog item has actually been FILED and its ID recorded — carrying the H2 determination from 0b, the H3 signal from 0a and the instrumentation, the full 0d table, and the path to the first-divergence dump — AND #934's re-scope note links to that ID. Verified by READING the backlog, not by asserting it. A hardening commit alone does not discharge branch (b).
(c) BOTH arms GREEN (G1 and G2) -> the bug was already resolved by the L319 conversion and the 8.975 A docstring records the historical PRE-fix state. Close #934 as already-resolved, land the hardening, add the cluster-marked regression test. The closure sub-rule from (a) applies in full.
(d) 0d-UNREACHABLE (NEW) -> the tie-point mechanism is falsified by arithmetic before any compute is spent. Do NOT run Stage 1 as specified. Land the five conversions as consistency-only hardening on a NON-CLOSING commit. #934 stays OPEN and re-scopes to H2/H3 plus the open question this branch exists to surface: if the tie point was never reachable at L319 either, WHY did converting L319 to floor(x+0.5) empirically fix an observed cluster divergence? Candidate answer to record for the follow-up: the two forms may differ by XLA lowering and fusion rather than by tie-breaking rule, in which case the entire hot/cold framing needs replacing. The positive-check gate from (b) applies in full. UNREACHABLE is not a tidy ending and must not be written up as one.

=== CLUSTER EXECUTION RISK BUDGET (NEW — backlog #4193) ===
A hang was observed on Engaging during #4167's VERIFY phase in tests/physics/test_settle_temperature_control.py, persisting even with JAX_PLATFORMS=cpu and thread caps. Stage 1's two targets are in test_settle_batched.py and test_settle_64water_batched_10ps.py — DIFFERENT files — so this is RISK-BUDGETING, NOT BLOCKING, and the pre-registered expectation is NO hang.
  Budget: submit Stage 1 with an explicit --time=02:00:00 and VERIFY the flag was actually inherited (myxcel --time / --partition non-inheritance bug observed 2026-08-11).
  Declare a hang if walltime is exceeded or the log shows no progress for 20 minutes.
  First hang: cancel and retry ONCE.
  Second hang: STOP spending cluster attempts. Mark Stage 1 BLOCKED, link #934 to #4193, and fall back to REPRO-FIRST scoped to the SMALLER target only — test_settle_batched.py::test_settle_batched_vs_unbatched (line 449), NOT the 20k-step 64-water run.
  If that also hangs: ESCALATE to the user. Do not iterate further.
  Observing a hang in either of these two files is itself a finding and must be attached to #4193.
Do not run the 20k-step suite on the local WSL2 box. A local green is not evidence of anything — the bug is DEFINED as "passes locally, fails on Engaging". Submit via myxcel/sbatch per project rules.
Regression guard before merge: confirm the dt=1.0 fs cap-lift gate is undisturbed (reference job 19774893).

=== ACCEPTANCE CRITERIA — the "Then" checklist (REPLACES v1's unfilled "_add specific measurable criteria_" placeholder) ===
AC1. Stage 0a recorded: the box vector for BOTH target tests, 8.975 A classified as half-box / one-box / arbitrary, and the resulting H1-vs-H3 re-weighting stated in writing.
AC2. Stage 0b recorded per test: (i) homogeneous-vs-padded determination (water_mask state; per-replica n_atoms/n_waters) and (ii) in-scan-body key-derivation determination; H2 marked EXCLUDED or LIVE with the criterion that decided it named. gamma alone is NOT accepted as the reason.
AC3. Stage 0c recorded: a full classified table of every jnp.round / jnp.mod hit in settle.py, reconciled against the six-site enumeration above, each marked in-traced-path or not.
AC4. Stage 0d recorded: per-site DERIVED attainable-|delta| range divided by box, an explicit REACHABLE / UNREACHABLE verdict per site, the L1277/1329 entry derived rather than inherited, and one overall verdict selecting Stage 1 or branch (d).
AC5. Conversions landed at L171, L773, L776, L1277, L1329 — or, per site, a written COLD argument in the work order; L161's stale round()-advocating sentence removed; dt=1.0 fs gate (job 19774893) confirmed undisturbed, or the commit split per site.
AC6. Rewritten p5_settle_batch_divergence.bth.toml: [experiment] schema, exactly one is_residual = true outcome, gating max_abs_atom_position_delta < 1e-10 A, with the stale settle.py:237 reference and the "root cause confirmed" decision text both removed.
AC7. Stage 1 executed, or branch (d) taken: A/B via import-time module-level constant across two back-to-back subprocesses in one slurm job; first-divergence step and per-site delta/box dump produced; G1 (or its 0d-VOID status) and G2 both reported; walltime and hang budget honoured.
AC8. Exactly one of branches (a)/(b)/(c)/(d) executed and NAMED in the closing note; if (a) or (c), the closure sub-rule is discharged for all four presumptively-hot sites and the causal claim is not overstated beyond what the dump shows; if (b) or (d), #934 is left OPEN and the positive-check gate is discharged with the new backlog item's ID recorded.

=== INVEST-S SPLIT TRIGGERS (scope grew this revision; S is a boundary pass at exactly eight criteria) ===
Split this item IMMEDIATELY if either fires: (1) a fixer is investigating jnp.linalg.eigh numerics inside this item; (2) Stage 0d turns into a research task rather than an arithmetic table — 0d is bounded at deriving five ranges, and if deriving the L1277/1329 range requires instrumented measurement rather than reasoning, that measurement becomes its own item and 0d records UNRESOLVED for that site, treating it as presumptively REACHABLE for branch selection.
created_at: 2026-08-12T14:33:25.094098+00:00
---

# Brainstorm: Decide the fix approach for prolix backlog #934 (P5-C) batched SETTLE vmap divergence — REVISION 2, superseding session 33ef1b3b

## Problem Frame
Frame confirmed as stated above — FIXED / NEGOTIABLE / stress-test-assumption / INVEST-S split trigger. Proceed to divergence.

## Idea Pool
- [user] SCOPE-EXPAND: Fix scope grows from L171 alone to five sites (L171, L773, L776, L1277, L1329). Justification is that v1's own reason for rejecting DETERMINISM-SWEEP was "changing untested sites OUTSIDE the failing test's blast radius" — but L773/776/1277/1329 are INSIDE it, invoked unconditionally by settle_langevin(...).apply_fn whenever water_indices is set, which both Stage 1 tests do. So the prior decision argues FOR converting them, not against. Requires assumption: converting the R-step and AM-restoration sites does not disturb the P5 C3 angular-momentum behaviour (678c9cb) — which must be checked against the dt=1.0 fs gate, job 19774893.
- [user] REACHABILITY-GATE (Stage 0d): jnp.round and floor(x+0.5) can only disagree when x = delta/box is within ULP of a HALF-INTEGER. Make a per-site attainable-|delta|-range table a BLOCKING zero-compute gate before the cluster job, with a pre-registered branch (d) for the UNREACHABLE outcome. Requires assumption: the attainable ranges can actually be bounded by inspection rather than measurement — true for L171 (sub-Angstrom SETTLE correction) and L773/776 (rigid O-H bond), less obviously true for L1277/1329 where dx depends on how far shift_fn wrapped.
- [user] BOTH-GATES-GREEN: Resolve the GREEN contradiction by making BOTH the constructed adversarial case (G1) and N>=5 random seeds (G2) mandatory and necessary, rather than choosing between v1's two contradictory statements. Strictly stronger than either, removes the ambiguity in one sentence, and costs almost nothing because both run inside the same job.
- [user] TOL-1e-10 on max_abs_atom_position_delta: name a single metric — max over atoms and Cartesian components of |x_loop - x_vmap| in Angstrom, evaluated per step, with FIRST-DIVERGENCE STEP the smallest index exceeding it — and set the tolerance at 1e-10 A. Explicitly supersede the sidecar's 1e-5, on the ground that 1e-5 gated a different question (CPU float64 unwrap stability) and is five orders too loose to catch slow accumulation. Requires assumption: legitimate float64 reassociation noise stays well below 1e-10 A over the trajectory lengths in play.
- [user] IMPORT-TIME-AB: Resolve the "no traced predicate" vs "SAME process" contradiction by mandating import-time selection via a module-level constant, with arm A and arm B as two back-to-back SUBPROCESS invocations inside one slurm job on one node. Relaxes v1's "SAME process" to "same job, same node, back-to-back" while preserving everything that wording bought (same machine, same seed, no cross-run confound). Requires assumption: an import-time constant genuinely produces a single fused traced path with no branch — which is the pre-mortem's own stated mitigation.
- [user] PADDING-NOT-GAMMA: Rewrite the H2 exclusion criterion to test padding rather than gamma. H2 is excluded IFF (i) the bundle is homogeneous — water_mask None or all-True, identical per-replica n_atoms/n_waters, so no padded row can advance lax.scan differently — AND (ii) inside the scan BODY each replica's key derives purely from its own carry leaf with no dependence on water_mask or another replica's data. gamma is irrelevant to the exclusion. Requires assumption: test_settle_64water_batched_10ps.py really is unpadded and uniform (stated: gamma=10, no water_mask), which 0b must verify rather than inherit.
- [user] HANG-BUDGET-4193: Give Stage 1 an explicit walltime and a pre-declared hang protocol keyed to backlog #4193's Engaging hang. Retry once, then fall back to REPRO-FIRST scoped to the smaller target only, then escalate — never iterate cluster attempts indefinitely. Requires assumption: #4193's hang is file-specific (it was observed in tests/physics/test_settle_temperature_control.py, a different file from Stage 1's two targets), making this risk-budgeting rather than blocking.
- [user] FILED-NOT-CLAIMED: Close pre-mortem FAILURE 2 by making branch (b)'s discharge a positive check — the follow-up backlog item must EXIST with a recorded ID carrying the H2/H3/0d findings, and #934 must link to it — verified by reading the backlog, not asserted. A hardening commit alone does not discharge branch (b).
- [user] CLOSURE-SUB-RULE: Gate branch (a)/(c) closure on every in-path box-normalised site being either converted or affirmatively argued COLD with explicit delta-magnitude reasoning, with L773/776/1277/1329 flagged PRESUMPTIVELY HOT so that a default defer is not an acceptable exclusion.
- [user] SPLIT-TRIGGER-S: Keep INVEST S passing by holding the checklist to exactly eight acceptance criteria and naming two breach signals — investigating eigh numerics inside this item, or Stage 0d turning into a research task rather than an arithmetic table.
- [user] REVERSAL A — reverse "my half-integer arithmetic is right, so the mechanism is unreachable": assume instead it is WRONG, and ask what would have to be true. Three ways it could be wrong, all cheap for the fixer to check and all of which belong in the 0d table rather than in my head. (1) The disagreement is not about the tie point at all: jnp.round and floor(x+0.5) lower to different XLA instruction sequences, and the extra addition in floor(x+0.5) changes where the value is materialised versus fused, so the two forms can disagree by an ULP in x itself, anywhere — not only at half-integers. Under that reading no site is safe by range argument and the whole hot/cold framing (v1's and mine alike) is misconceived. (2) delta at L1277/1329 is not bounded the way I assumed: dx = x_con - x_unc where shift_fn has already wrapped x_unc, so dx is a difference of two independently-wrapped quantities and its attainable range may genuinely cover box/2. My bound was asserted, not derived. (3) The L319 empirical fix may have worked for an unrelated reason (e.g. it also changed operation count and therefore fusion), in which case the recorded comment attributing it to banker's rounding is itself the unverified claim. This reversal is why 0d must be a CHECK with a pre-registered branch, never a conclusion I hand the fixer.
- [user] REVERSAL B — reverse "the constructed adversarial case is buildable": assume it is NOT. If 0d returns UNREACHABLE at every site, then no physically valid initial condition drives any site to the tie point, G1 cannot be satisfied or falsified, and a GREEN that requires G1 becomes unsatisfiable — the plan would deadlock. This is the failure mode v1 could not see because it never asked whether the regime was reachable. Consequence for the artifact: G1 must be made conditional on 0d-REACHABLE, and branch (d) must exist to absorb the UNREACHABLE case, or the acceptance criteria contain an unclosable item. This is a genuine internal-consistency defect the checklist did not name, and it is the direct product of fixing checklist item 4.
- [user] REVERSAL C — reverse "expanding scope to five sites is obviously right": assume it is WRONG. L773/776 sit inside _r_step_conserve_angular_momentum, the exact code the P5 C3 work (678c9cb) added to stop T_rot drifting, and L1277/1329 sit in the R-step momentum correction whose comment says a mis-handled displacement "detonates the integrator at liquid density". Changing rounding behaviour in both, in the same commit, on the dt=1.0 fs critical path, is not a cosmetic sweep — it is a behavioural change to the two most delicate blocks in the file. If the conversions are landed together with the L171 change and something regresses, the A/B cannot attribute it. Mitigation the artifact must carry: the five conversions land as ONE reviewable change whose regression guard is the dt=1.0 fs gate (job 19774893), and the work order must state that a regression there means splitting the commit per site, not proceeding.
- [user] REVERSAL D — reverse "the artifact should be a v2 file": assume superseding cleanly matters more than filename. If v1's spec stays in .praxia/docs/specs/ with no supersession marker, a future reader can execute the contradictory v1 criteria in good faith — which is exactly the pre-mortem FAILURE 2 pattern applied to documentation rather than to backlog state. The artifact must therefore carry the superseded session id and v1 path in its own text, so the linkage survives even if only one file is read.
- [user] converge
- [user] converge — the option space is enumerated and the reversals have surfaced the one defect the checklist did not name (REVERSAL B: G1 is unsatisfiable if 0d returns UNREACHABLE, so branch (d) is structurally required, not optional). Ready to rank and eliminate.

## Decision Log
- [REJECT] Ship the preliminary half-integer reachability arithmetic as a stated FINDING that pre-decides the mechanism: Rejected on the same grounds that got v1 sent back for revision. v1's REVERSAL 2 asserted a magnitude argument ("delta ~= box, so delta/box ~= +/-0.5") that is arithmetically wrong — delta ~= box gives x ~= 1.0 — and then built a hot/cold narrative on it that selected the single cold site as the fix target. Repeating that pattern with my own quickly-derived table would reproduce the defect at a higher level of confidence, which is worse. Two legs of my table are weak: the L1277/1329 bound treats dx = x_con - x_unc as "either ~0 or ~box" without deriving it, when it is a difference of two independently-wrapped quantities and may genuinely cover box/2; and the whole tie-point framing may be beside the point if jnp.round and floor(x+0.5) differ by lowering to different XLA instruction sequences rather than by tie-breaking rule. The table therefore ships as an explicitly-labelled HYPOTHESIS inside a blocking zero-compute check (Stage 0d) with a pre-registered branch for each outcome, and the artifact must name L1277/1329 as the weakest entry requiring derivation rather than inheritance.
- [ACCEPT] Branch (d) for the 0d-UNREACHABLE outcome, and making G1 (constructed adversarial case) conditional on 0d-REACHABLE: Structurally required, not optional — this is the one defect the ten-item checklist did not name, and it falls directly out of fixing checklist item 4. Item 4 demands a concrete construction recipe for an adversarial case driving delta/box to the tie point. Attempting to write one exposed that the regime may be physically unreachable through the real call graph at every site. If it is, then G1 can be neither satisfied nor falsified, and an acceptance-criteria block that makes G1 mandatory unconditionally contains an unclosable item — the exact class of defect this revision exists to remove. So G1 is mandatory IFF Stage 0d returns REACHABLE, and branch (d) absorbs the UNREACHABLE case: the five conversions land as consistency-only hardening on a NON-CLOSING commit, #934 stays OPEN, and the positive-check gate applies in full. Branch (d) must also name the resulting open question rather than treating UNREACHABLE as a tidy ending: if the tie point was never reachable at L319 either, why did converting L319 to floor(x+0.5) empirically fix an observed cluster divergence? That question is the most valuable thing this item could surface and it must not be lost.
- [MERGE] v1's "SAME process" A/B framing, and v1's two contradictory GREEN pass-criteria (Evidence Rule N>=5 seeds vs Decision Log constructed-case-primary): Both are replaced by single authoritative statements in the operative text, because in each case v1 said two incompatible things and buried the more important one where a fixer would not read it. (1) "SAME process" is RELAXED to "same job, same node, same allocation, back-to-back subprocesses, same seed, same box, same git SHA". v1 simultaneously required "no traced predicate, no in-path branch" and "SAME process", but the pre-mortem's own FAILURE-1 mitigation names import-time selection or separate process invocations as the safe mechanism — which "SAME process" forbids. The relaxation preserves everything the original wording bought (one machine, one seed, no cross-run confound) while permitting a module-level constant bound at import scope, so each traced program contains exactly one fused code path. (2) GREEN now requires BOTH G1 (constructed adversarial case, conditional on 0d-REACHABLE) AND G2 (N>=5 random seeds), each necessary, neither sufficient — rather than choosing between v1's Evidence Rule (seeds only, constructed case unmentioned) and v1's Decision Log ACCEPT note (constructed case primary, seeds supplementary). Requiring both is strictly stronger than either, removes the ambiguity in one sentence, and costs almost nothing because both run inside the same job. Failure of either alone is conclusive red.

## Assumptions

## TBDs

## Pre-mortem Record
**User:** Six months on, this revision has definitively failed. Most likely stories, in order:

FAILURE 1 (most likely, and it is a failure OF THE REVISION rather than of the plan): the artifact was too long. It resolved all ten defects correctly, but at a length where a fixer read the Stage 0 section, started work, and never reached the branch table or the closure sub-rule. Branch (a) was taken on a green run, #934 was closed, and L773/776/1277/1329 were never converted — the exact outcome the closure sub-rule exists to prevent, defeated by the sub-rule being on page three. v1 failed by being contradictory; v2 fails by being unread. Mitigation the handoff must carry: AC1-AC8 is the operative contract and must be reproduced at the TOP of the work order, not only at the bottom of the spec. If only one part of this artifact survives into the fixer's context, it must be the eight-line checklist plus the closure sub-rule.

FAILURE 2: Stage 0d was answered lazily. It is the one new gate and the only one requiring genuine derivation rather than lookup. A fixer under critical-path pressure writes "L1277/1329: dx is either ~0 or ~box, UNREACHABLE" — copying my preliminary table verbatim, which the artifact explicitly warns is the weakest entry — takes branch (d), lands five conversions as cosmetic hardening, and closes out. The live bug survives behind a tidy arithmetic argument that was never actually derived. This is worse than v1's failure mode because it produces a confident-sounding negative result. Mitigation: AC4 must require the L1277/1329 range be shown with working, and the split trigger must fire (0d records UNRESOLVED, site treated as presumptively REACHABLE) rather than allowing an unshown UNREACHABLE.

FAILURE 3: converting L773/776 broke angular-momentum conservation subtly. Not enough to fail the dt=1.0 fs gate at job-19774893 scale, but enough to introduce a slow T_rot drift that only appears in long production runs months later — and because five sites changed in one commit, nobody could attribute it. The regression guard was real but not sensitive enough. Mitigation: the pre-declared response to a regression (split per site and bisect) must also apply to a MARGINAL result, not only a clear failure, and the P5 C3 unit test for AM conservation should be named explicitly alongside the gate job.

FAILURE 4: the whole mechanism story was wrong in the way REVERSAL A(1) suggests — round versus floor differ by XLA lowering and fusion, not by tie-breaking — and both v1 and v2 spent their effort on range arithmetic that was never the relevant question. Branch (d) names this, which is why it is not fatal, but the plan still funnels the fixer through four stages of tie-point reasoning before it lets them consider it. Mitigation: branch (d)'s open question must be filed as its own backlog item with the H2/H3 findings, not left as a paragraph in a closed spec.

FAILURE 5 (lower probability): TOL=1e-10 was too tight. Legitimate accumulation over 20k steps at 64 waters exceeded it, every arm came back red, branch (b) fired mechanically, and the item split into a follow-up chasing numerical noise. Mitigation: the first-divergence dump distinguishes these cases by construction — a box-quantised primitive error versus a ~1e-10 arbitrary one — so the artifact's requirement to read the dump before assigning a branch is doing real work here and must not be skipped. If the observed primitive error is order 1e-10 and arbitrary, that is evidence to revisit TOL, not evidence of a wrap bug.

None of these overturn the winner. Failures 1 and 2 are serious enough that they must appear as explicit constraints in the handoff rather than as afterthoughts.
**AI:** _not recorded_

## Acceptance Criteria
**Given** Frame confirmed as stated above — FIXED / NEGOTIABLE / stress-test-assumption / INVEST-S split trigger. Proceed to divergence.
**When** implementing STAGED-BISECT v2 — REPLACEMENT ACCEPTANCE-CRITERIA BLOCK for backlog #934 (P5-C).

SUPERSESSION NOTICE: this block SUPERSEDES contemplex session 33ef1b3b and its artifact .praxia/docs/specs/260812_decide-the-fix-approach-for-prolix-backl.md ("v1"). v1 is locked in Phase 4 and could not be amended in place. Where this block and v1 disagree, THIS BLOCK GOVERNS. Do not execute v1's acceptance criteria: they end in an unfilled template placeholder and contain two mutually contradictory GREEN pass-criteria. This block is standalone — a fixer needs nothing from v1.

APPROACH (settled in round 0 by independent challenger and defender review; NOT reopened): STAGED-BISECT — zero-compute discriminators first, then ONE instrumented A/B cluster job, with an asymmetric red/green evidence rule. TOLERANCE-DOC stays REJECTED. H1-ELIMINATE (deleting settle_positions' second re-wrap) stays DEFERRED. Cluster re-validation stays MANDATORY. The rounding conversions land as HARDENING, never as a confirmed cause.

WHAT THIS REVISION CHANGED, AND WHY:
(A) Fix scope was wrong. settle.py has SIX box-normalised rounding sites, not the two v1 discusses; four previously-undiscussed ones sit in the identical traced path as both Stage 1 tests.
(B) The hot/cold arithmetic was mis-stated on both sides, which produces a new blocking zero-compute gate (Stage 0d) and a fourth branch.
(C) v1's "SAME process" A/B framing directly contradicts its own "no traced predicate" hard constraint; it is relaxed.

=== VERIFIED SITE ENUMERATION (read directly from src/prolix/physics/settle.py during this revision) ===
  L171   settle_positions                     -box * jnp.round(delta_o / box)   delta_o = pos_O_c - pos_O_new (sub-Angstrom SETTLE geometric correction). THE ONLY SITE v1 TARGETS.
  L319   _settle_water_batch                  delta - box * jnp.floor(delta/box + 0.5)   ALREADY CONVERTED
  L773   _r_step_conserve_angular_momentum    dH1 - box * jnp.round(dH1 / box)   dH1 = H1 - O, atoms independently wrapped by shift_fn
  L776   _r_step_conserve_angular_momentum    dH2 - box * jnp.round(dH2 / box)
  L1277  settle_langevin R1-step              dx_1 - box * jnp.round(dx_1 / box)   dx_1 = x_con_1 - x_unc_1
  L1329  settle_langevin R2-step              dx_2 - box * jnp.round(dx_2 / box)
L773/776/1277/1329 are invoked UNCONDITIONALLY by settle_langevin(...).apply_fn whenever water_indices is not None — true of BOTH Stage 1 target tests — so they are INSIDE the failing test's blast radius. v1 rejected DETERMINISM-SWEEP's blanket conversion specifically because it would touch sites OUTSIDE that radius; that reasoning does not shield these four, it argues for converting them. Their own inline comments document the hazard: L737-741 "water molecules near a periodic boundary may have O at x~0 and H at x~L_box ... catastrophically large impulse -> NaN"; L1268-1274 "the raw difference can jump by a full box vector ... a spurious ~box-sized impulse that detonates the integrator at liquid density".

=== STAGE 0 — ZERO COMPUTE, ALL FOUR BLOCKING PREREQUISITES ===
(0a) ARITHMETIC DISCRIMINATOR. Read the box vector from tests/physics/test_settle_batched.py::test_settle_batched_vs_unbatched (line 449) and from tests/physics/test_settle_64water_batched_10ps.py. NOTE: v1 wrote "tests/physics/test_settle_batched_vs_unbatched" as though it were a file path; it is a FUNCTION inside test_settle_batched.py. Compare 8.975 A against the box: ~17.95 -> half-box, minimum-image SIGN disagreement (H1 family); ~8.975 -> clean one-box jump (H1 family); >=30 -> arbitrary, wrap story WEAK, re-weight toward H3/eigh. An AMBIGUOUS result is itself informative and re-weights toward H3 — it must be recorded, not shrugged off.

(0b) H2 EXCLUSION — CRITERION CORRECTED. v1's criterion ("gamma=0 OR keys split before the scan") does not test what H2 hypothesises. H2 says PADDED rows advance lax.scan a different number of times than unpadded rows, desynchronising per-step noise draws. The discriminator is PADDING, not gamma. H2 is structurally excluded IFF BOTH hold:
   (i) the bundle is HOMOGENEOUS — water_mask is None or all-True, and every replica has identical n_atoms / n_waters — so no padded row exists that could advance the scan differently; AND
   (ii) INSIDE the scan BODY, each replica's per-step key derives purely from its own carry leaf (e.g. key, subkey = jax.random.split(carry.key) on a per-replica vmapped carry), with NO dependence on water_mask, on any padded-row quantity, or on another replica's data.
Record (i) and (ii) explicitly for each test. Checking only what happens BEFORE the scan is insufficient. Input to VERIFY, not inherit: test_settle_64water_batched_10ps.py reportedly runs gamma=10 (NONZERO) with NO padding and no water_mask (uniform batch, both replicas the identical system) — if so it is excluded by (i), and would NOT have been excluded by v1's gamma clause. Apply the same two-part check to test_settle_batched_vs_unbatched (line 449).
If (i) fails for either test, H2 stays LIVE and gets a gamma=0 control arm in Stage 1: divergence vanishes -> H2 implicated; persists -> H2 excluded.

(0c) SCOPE GREP. rg 'jnp\.round\(|jnp\.mod\(' src/prolix/physics/settle.py. Classify every hit twice: box-normalised or not, and inside the traced apply_fn under test or not. The six-site table above is the expected result — reconcile any discrepancy before proceeding. settle_velocities (L850) takes no box parameter and is structurally exempt.

(0d) REACHABILITY ARITHMETIC — NEW, BLOCKING, and the cheapest available falsifier. jnp.round and floor(x+0.5) can only disagree by tie-breaking rule when x = delta/box lies within ULP of a HALF-INTEGER (|delta| = box/2, 3*box/2, ...). For each of the five unconverted sites, DERIVE the physically attainable range of |delta|, divide by the 0a box, and state whether any half-integer falls inside it.
   PRELIMINARY HYPOTHESIS — TO BE CONFIRMED OR REFUTED BY THE FIXER, NOT ASSUMED TRUE: none of the five appears to reach a half-integer. L171 gives x ~ 0.01 (sub-Angstrom SETTLE correction over a >=10 A box); L1277/L1329 give x ~ 0 (no wrap) or x ~ 1.0 (wrap); L773/L776 give x ~ 0.05 (unwrapped O-H bond) or x ~ 0.95 (wrapped). THE L1277/L1329 ENTRY IS THE WEAKEST AND MUST BE DERIVED, NOT INHERITED: dx = x_con - x_unc is a difference of two INDEPENDENTLY-wrapped quantities (shift_fn wraps x_unc; settle_positions reconstructs x_con in the unwrapped frame of positions_old), so its attainable range may genuinely cover box/2.
   This hypothesis also CORRECTS v1: v1's REVERSAL 2 wrote "delta ~= box, so delta/box ~= +/-0.5" to justify calling L319 hot. That is wrong arithmetic — delta ~= box gives x ~= 1.0, not 0.5. So v1's stated reason for L319 being hot does not hold as written, even though converting L319 empirically fixed an observed cluster divergence. That tension is real and is carried into branch (d), not resolved here.
   COUNTER-HYPOTHESIS the fixer must also weigh: the two forms may differ NOT by tie-breaking rule but because they lower to different XLA instruction sequences — the extra addition in floor(x+0.5) changes where the value is materialised versus fused, so the two can disagree by an ULP in x itself, ANYWHERE, not only at half-integers. Under that reading every range argument in both plan versions is beside the point. This reading is arguably MORE consistent with the L319 empirical result than the tie-point story is.
   PRE-REGISTERED OUTCOMES:
     0d-REACHABLE — a half-integer lies within some in-path site's derived range. That site is the target of the mandatory constructed adversarial case (G1). Proceed to Stage 1.
     0d-UNREACHABLE — no half-integer at any in-path site. BRANCH (d) fires. Do NOT submit the Stage 1 cluster job as specified.

=== CONSTRUCTED ADVERSARIAL CASE (G1) — RECIPE ===
Applicable only under 0d-REACHABLE. Build it at the site 0d identifies, through the REAL settle_langevin(...).apply_fn call graph — not by calling an internal helper directly, which would not exercise the traced path under test.
  If the site is L773/L776 (dH = H - O): construct a water straddling a periodic face — oxygen placed just inside the +x face, both hydrogens placed so that shift_fn's independent per-atom wrap lands them across it — and tune the O-to-face offset so that |dH_x| / L_x lands on the half-integer 0d identified. The box-to-bond-length ratio is the free parameter: the required |dH| is box/2, so the construction is feasible only for the box sizes 0d certifies.
  If the site is L1277/L1329 (dx = x_con - x_unc): drive the water across the face on the step SETTLE acts, choosing dt and the initial velocity along x so that the A-step displacement plus the SETTLE correction places |dx_x| at box/2. Report dt and the velocity used.
  If the site is L171 (delta_o): NOT a plausible target. v1's own REVERSAL 2 argues delta_o is a sub-Angstrom geometric correction, so reaching the tie point there requires the constrained position to have already moved ~half a box, meaning something upstream failed first. v1's language implying L171 is the adversarial target is WITHDRAWN. If 0d nonetheless certifies L171 as reachable, that finding itself supersedes REVERSAL 2 and must be recorded as such.
  The constructed case runs deterministically (fixed seed, gamma may be set to 0 for it) and is asserted under the same TOL as the seeded cases.

=== THE FIX ===
Convert to the deterministic idiom, mirroring the already-converted L319: replace X - box * jnp.round(X / box) with X - box * jnp.floor(X / box + 0.5).
  MANDATORY at L171, L773, L776, L1277, L1329 — unless an individual site is affirmatively argued COLD in the work order using explicit delta-magnitude reasoning and the 0d table. A default defer is NOT an acceptable exclusion for L773/776/1277/1329: they are PRESUMPTIVELY HOT on the strength of the code's own adjacent comments, and excluding any of them requires a written argument.
  Also remove/replace the stale sentence at L161 — "The only exception is a genuine PBC crossing (|delta| ~ box), handled by round()" — which argues FOR round() in precisely the regime under investigation.
  LAND AS ONE REVIEWABLE CHANGE with the dt=1.0 fs cap-lift gate (reference job 19774893) as the regression guard. L773/776 sit inside _r_step_conserve_angular_momentum, the code the P5 C3 work (678c9cb) added to stop T_rot drifting; L1277/1329 sit in the R-step momentum correction. These are the two most delicate blocks in the file, not a cosmetic sweep. PRE-DECLARED RESPONSE TO A REGRESSION: split the commit per site and bisect — do NOT proceed with the combined change.
  The commit message must state the mechanism per the 0d result and must NOT assert the fix is the confirmed cause.

=== STAGE 1 — ONE CLUSTER JOB (only under 0d-REACHABLE) ===
Extend scripts/experiments/p5_settle_batch_divergence.py; do not write a new diagnostic.

A/B CODE SHAPE — resolves v1's contradiction between "no traced predicate" and "SAME process". REQUIRED: select the rounding idiom at MODULE IMPORT TIME via a module-level constant in settle.py (e.g. _PBC_ROUND, bound once at import scope from an environment variable), so each traced program contains exactly ONE fused code path with no branch. Arm A (round) and arm B (floor) are two back-to-back SUBPROCESS invocations from the SAME slurm job, SAME node, SAME allocation, SAME seed, SAME box, SAME git SHA, differing only in that environment variable.
  FORBIDDEN: any bool or array flag threaded into a traced function; any lax.cond or Python branch inside the traced body; any environment-variable read inside the traced body; any change to settle_positions' signature.
  v1's "SAME process" wording is RELAXED to "same job, same node, back-to-back". It contradicted v1's own pre-mortem FAILURE-1 mitigation, which names import-time selection or separate process invocations as the safe mechanism. Everything "same process" was buying — one machine, one seed, no cross-run confound — is preserved.
  If even import-time selection is judged to perturb lowering, FALL BACK to REPRO-FIRST (run both target tests unmodified at HEAD) and accept the extra queue round-trip.

METRIC — single, named, closable: max_abs_atom_position_delta = max over atoms i and Cartesian components c of |x_loop[i,c] - x_vmap[i,c]|, in Angstrom, evaluated per step. FIRST-DIVERGENCE STEP = the smallest step index at which this exceeds TOL. Terminal-trajectory RMSD is explicitly NOT the gate metric — it measures the amplified cascade, not the primitive error. At first divergence, dump: offending molecule index; its pre/post O/H1/H2 positions on both paths; the value of delta/box at EVERY one of the five sites for that molecule; and whether the primitive |delta| is near an integer or half-integer multiple of box.

TOL = 1e-10 Angstrom, applied to max_abs_atom_position_delta. Justification: the frame requires loop/vmap equivalence; a wrap-class error is box-quantised (order 1-20 A) while legitimate float64 reassociation noise at Angstrom-scale coordinates is order 1e-15 A per operation, accumulating to at most ~1e-12 A over the trajectory lengths in play. 1e-10 A therefore sits several orders above achievable round-off and ten-plus orders below any wrap error, so no result is ambiguous and no "small but real" divergence can be waved through.

SIDECAR — the EXISTING scripts/experiments/p5_settle_batch_divergence.bth.toml is SUPERSEDED and must be REWRITTEN as part of this item; it cannot be reused. Three reasons, all verified by reading it: (1) it gates on max_diff_with_box < 1e-5, a threshold chosen for a different question ("is the CPU float64 box unwrap stable?") and five orders too loose to detect slow accumulation; (2) its hypothesis string still cites "settle.py:237" for the _settle_water_batch site, now L319 — stale; (3) its pass-branch decision text reads "platform_precision root cause confirmed", a conclusion the run it gates cannot establish — exactly the post-hoc reading the asymmetric evidence rule exists to prevent. The rewrite keeps the [experiment] schema with exactly one outcome carrying is_residual = true, and encodes TOL = 1e-10 on max_abs_atom_position_delta plus the branch conditions below. Run under bth run inside a slurm wrapper sourcing scripts/slurm/_bth_env.sh; bth campaign create BEFORE the first run; pass --campaign to every run.

=== EVIDENCE RULE — THE ONE AUTHORITATIVE STATEMENT (overrides every other phrasing, in v1 or elsewhere) ===
RED is conclusive on a SINGLE observation. Any arm exceeding TOL on any case is red.
GREEN requires BOTH of the following. Each is necessary; neither alone suffices:
   (G1) the CONSTRUCTED adversarial case passes under TOL — MANDATORY WHENEVER 0d RETURNS REACHABLE, and it is the primary evidence, because it proves the identified hot regime is deterministic. If 0d returns UNREACHABLE, G1 is VOID (not waived) and branch (d) applies instead of the green/red branches.
   (G2) N >= 5 random seeds all pass under TOL — ALSO MANDATORY, because it provides evidence that no OTHER regime is hot.
Failure of G1 alone is conclusive red. Failure of G2 alone is conclusive red. Both run inside the same job, so requiring both costs nearly nothing.
This paragraph REPLACES v1's Evidence Rule section (which required N>=5 seeds and never mentioned a constructed case) AND v1's Decision-Log ACCEPT note (which said the constructed case was primary and multi-seed merely supplementary). Those two were contradictory; neither survives.

=== BRANCH ACTIONS — pre-declared, exhaustive, exactly one executes ===
(a) Post-fix arm GREEN (G1 and G2) AND pre-fix arm RED -> the fix is sufficient and CAUSAL. Land it, add a cluster-marked regression test, close #934 — SUBJECT TO THE CLOSURE SUB-RULE.
    CLOSURE SUB-RULE (blocking): #934 may close only if EVERY box-normalised round()/mod() site enumerated at 0c that lies within the traced apply_fn under test is either (i) converted alongside L171, or (ii) affirmatively argued COLD in the work order using explicit delta-magnitude reasoning and the 0d table. L773, L776, L1277 and L1329 are specifically flagged PRESUMPTIVELY HOT: excluding any of them requires a written argument, not a default defer.
    ADDITIONAL BLOCK ON THE CAUSAL CLAIM: do not write "banker's rounding was the cause" unless the first-divergence dump actually shows a box-quantised primitive error. If the primitive error is arbitrary-magnitude, the fix may have worked for a different reason and the closing note must say so.
(b) BOTH arms RED -> the fix is necessary-but-insufficient. Land the hardening as a NON-CLOSING commit. #934 stays OPEN and re-scoped. Read the first-divergence dump: box-quantised primitive error -> an unfound wrap site (feed the 0c list); arbitrary magnitude -> H3/eigh.
    POSITIVE-CHECK GATE (blocking; closes pre-mortem FAILURE 2): branch (b) is discharged only when a NEW backlog item has actually been FILED and its ID recorded — carrying the H2 determination from 0b, the H3 signal from 0a and the instrumentation, the full 0d table, and the path to the first-divergence dump — AND #934's re-scope note links to that ID. Verified by READING the backlog, not by asserting it. A hardening commit alone does not discharge branch (b).
(c) BOTH arms GREEN (G1 and G2) -> the bug was already resolved by the L319 conversion and the 8.975 A docstring records the historical PRE-fix state. Close #934 as already-resolved, land the hardening, add the cluster-marked regression test. The closure sub-rule from (a) applies in full.
(d) 0d-UNREACHABLE (NEW) -> the tie-point mechanism is falsified by arithmetic before any compute is spent. Do NOT run Stage 1 as specified. Land the five conversions as consistency-only hardening on a NON-CLOSING commit. #934 stays OPEN and re-scopes to H2/H3 plus the open question this branch exists to surface: if the tie point was never reachable at L319 either, WHY did converting L319 to floor(x+0.5) empirically fix an observed cluster divergence? Candidate answer to record for the follow-up: the two forms may differ by XLA lowering and fusion rather than by tie-breaking rule, in which case the entire hot/cold framing needs replacing. The positive-check gate from (b) applies in full. UNREACHABLE is not a tidy ending and must not be written up as one.

=== CLUSTER EXECUTION RISK BUDGET (NEW — backlog #4193) ===
A hang was observed on Engaging during #4167's VERIFY phase in tests/physics/test_settle_temperature_control.py, persisting even with JAX_PLATFORMS=cpu and thread caps. Stage 1's two targets are in test_settle_batched.py and test_settle_64water_batched_10ps.py — DIFFERENT files — so this is RISK-BUDGETING, NOT BLOCKING, and the pre-registered expectation is NO hang.
  Budget: submit Stage 1 with an explicit --time=02:00:00 and VERIFY the flag was actually inherited (myxcel --time / --partition non-inheritance bug observed 2026-08-11).
  Declare a hang if walltime is exceeded or the log shows no progress for 20 minutes.
  First hang: cancel and retry ONCE.
  Second hang: STOP spending cluster attempts. Mark Stage 1 BLOCKED, link #934 to #4193, and fall back to REPRO-FIRST scoped to the SMALLER target only — test_settle_batched.py::test_settle_batched_vs_unbatched (line 449), NOT the 20k-step 64-water run.
  If that also hangs: ESCALATE to the user. Do not iterate further.
  Observing a hang in either of these two files is itself a finding and must be attached to #4193.
Do not run the 20k-step suite on the local WSL2 box. A local green is not evidence of anything — the bug is DEFINED as "passes locally, fails on Engaging". Submit via myxcel/sbatch per project rules.
Regression guard before merge: confirm the dt=1.0 fs cap-lift gate is undisturbed (reference job 19774893).

=== ACCEPTANCE CRITERIA — the "Then" checklist (REPLACES v1's unfilled "_add specific measurable criteria_" placeholder) ===
AC1. Stage 0a recorded: the box vector for BOTH target tests, 8.975 A classified as half-box / one-box / arbitrary, and the resulting H1-vs-H3 re-weighting stated in writing.
AC2. Stage 0b recorded per test: (i) homogeneous-vs-padded determination (water_mask state; per-replica n_atoms/n_waters) and (ii) in-scan-body key-derivation determination; H2 marked EXCLUDED or LIVE with the criterion that decided it named. gamma alone is NOT accepted as the reason.
AC3. Stage 0c recorded: a full classified table of every jnp.round / jnp.mod hit in settle.py, reconciled against the six-site enumeration above, each marked in-traced-path or not.
AC4. Stage 0d recorded: per-site DERIVED attainable-|delta| range divided by box, an explicit REACHABLE / UNREACHABLE verdict per site, the L1277/1329 entry derived rather than inherited, and one overall verdict selecting Stage 1 or branch (d).
AC5. Conversions landed at L171, L773, L776, L1277, L1329 — or, per site, a written COLD argument in the work order; L161's stale round()-advocating sentence removed; dt=1.0 fs gate (job 19774893) confirmed undisturbed, or the commit split per site.
AC6. Rewritten p5_settle_batch_divergence.bth.toml: [experiment] schema, exactly one is_residual = true outcome, gating max_abs_atom_position_delta < 1e-10 A, with the stale settle.py:237 reference and the "root cause confirmed" decision text both removed.
AC7. Stage 1 executed, or branch (d) taken: A/B via import-time module-level constant across two back-to-back subprocesses in one slurm job; first-divergence step and per-site delta/box dump produced; G1 (or its 0d-VOID status) and G2 both reported; walltime and hang budget honoured.
AC8. Exactly one of branches (a)/(b)/(c)/(d) executed and NAMED in the closing note; if (a) or (c), the closure sub-rule is discharged for all four presumptively-hot sites and the causal claim is not overstated beyond what the dump shows; if (b) or (d), #934 is left OPEN and the positive-check gate is discharged with the new backlog item's ID recorded.

=== INVEST-S SPLIT TRIGGERS (scope grew this revision; S is a boundary pass at exactly eight criteria) ===
Split this item IMMEDIATELY if either fires: (1) a fixer is investigating jnp.linalg.eigh numerics inside this item; (2) Stage 0d turns into a research task rather than an arithmetic table — 0d is bounded at deriving five ranges, and if deriving the L1277/1329 range requires instrumented measurement rather than reasoning, that measurement becomes its own item and 0d records UNRESOLVED for that site, treating it as presumptively REACHABLE for branch selection.
**Then**
  - [ ] _add specific measurable criteria_
