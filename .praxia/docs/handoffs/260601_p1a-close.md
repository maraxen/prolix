---
date: 2026-06-01
task_id: 260601_p1a_molecular_bundle
session_id: 260601-p1a-close
status: complete
branch: main
commit: 64249b7
phase: P1a → P2b / Phase 5 transition
---

# Handoff: P1a MolecularBundle Close

## Summary

Both session objectives delivered:

1. **NPT fix confirmed** — `scripts/debug/npt_step0_diagnostic.py` shows cold init
   T=260.6 K (expected 260–300 K; pre-fix was ~5000 K from the Bernetti-Bussi sign error
   fixed in commit b6e5bb9). No divergence after 1 warm step. Backlog #160 closed.

2. **P1a MolecularBundle closed** — Core implementation was already on main from prior
   sessions. The only gap: `test_shim_mode.py` failed to import `bond_forces` because the
   function was named `bond_forces_analytical` with a different argument order
   (`displacement_fn` before `bond_mask`), and the test used the wrong `[k, r0]` param
   convention + a missing 0.5 factor in the reference energy. Fixed both: added
   `bond_forces()` as a public adapter, corrected the test to use `[r0, k]` with
   `U = 0.5·k·(r−r0)²`. 22/22 tests pass on main (commit 64249b7). Backlog #163 closed.

---

## What happened this session

1. Read prior handoff (`260601_p0-parallel-close`) — confirmed NPT step 1 + P1a dispatch.
2. Committed lingering `audits.jsonl` record from prior fixer (clean working tree).
3. Ran NPT step-0 diagnostic → cold init T=260.6 K ✓.
4. Discovered P1a core already implemented; ran 18/22 tests, found 1 broken test file.
5. Diagnosed two bugs: naming mismatch + param convention mismatch in test.
6. Added `bond_forces()` adapter in worktree, fixed test convention, confirmed 22/22 pass.
7. Merged to main (commit 64249b7). Closed backlog #160 and #163.

---

## Next session: two candidates, recommend P2b first

Both are unblocked. Roadmap order is P0 → P1 → P2b → P3 → P5 → paper.

### Option A: P2b Cross-Validation Harness (recommended first)

**Why first:** Shorter (2–3 days), produces the paper's parity evidence. P1a + NPT are
now confirmed — the two gates for P2b are cleared.

**Entry point:** Read `.praxia/docs/audits/260526_p2a-bonded-field-audit.md` for the
field list. The bonded-path audit (PASS) and nonbonded-path audit (PASS, extended 260527)
both live in that doc.

**task_id:** `260601_p2b_crossval_harness`

**Dispatch:** `recon` → `specification-specialist` (spec harness against OpenMM) →
`fixer` → `auditor`

### Option B: Phase 5 Constraint-Aware Thermostat (paper critical path)

**Why P1:** Only known path to lifting the dt ≤ 0.5 fs constraint. LFMiddle was falsified
(campaign 89c9a900). 4–6 day effort. Must start no later than after P2b.

**Entry point:** `.praxia/docs/v1.1_next_steps.md` Phase 5 section.

**task_id:** `260601_phase5_constraint_thermostat`

---

## Immediately relevant files

| File | Lines | Why |
|---|---|---|
| `src/prolix/physics/analytical_forces.py` | 692–710 | `bond_forces()` public adapter (just added) |
| `src/prolix/types/bundles.py` | 1–197 | `MolecularBundle` + `MolecularShapeSpec` — complete |
| `src/prolix/types/shims.py` | 1–97 | `ShimMode` + `energy_with_analytical_shim` — complete |
| `src/prolix/physics/settle.py` | ~1935–1995 | NPT fix: line ~1944 `* mu`, line ~1985 `position` |
| `.praxia/docs/audits/260526_p2a-bonded-field-audit.md` | all | Field list for P2b harness |
| `.praxia/docs/v1.1_next_steps.md` | all | Roadmap: Phase 5 section |

---

## Failed attempts (do not retry)

### LFMiddle hypothesis (campaign 89c9a900)

FALSIFIED. 0/46 passes. DO NOT RETRY. Phase 5 (constraint-aware thermostat) is the
only known path to lifting the dt ≤ 0.5 fs constraint.

### `bond_forces = bond_forces_analytical` simple alias

Fails — arg order differs: test expects `(positions, idx, params, mask, disp_fn)` but
`bond_forces_analytical` signature is `(positions, indices, params, disp_fn, mask)`.
Also the test had `[k, r0]` param order and a missing 0.5 factor. All three fixed in
the committed solution.

---

## Roadmap state at handoff

```
[P0: CLEARED]
  ├─ LFMiddle dt-sweep     FALSIFIED (campaign 89c9a900, 2026-06-01)
  ├─ NPT KE init bug       RESOLVED (commit b6e5bb9, confirmed 2026-06-01)
  └─ test_npt_20ps         PENDING cluster/extended-timeout confirm (CPU too slow)

[P1a: CLOSED]
  └─ MolecularBundle       DONE (commits 72ad000..64249b7, 22/22 tests)

[P2b: NEXT RECOMMENDED]
  └─ Cross-validation harness — gates cleared (P1a + NPT both confirmed)

[Phase 5: P1 CANDIDATE after P2b]
  └─ Constraint-aware thermostat — 4-6 day, paper critical path

[Paper: 6-week critical path]
  └─ P0 → P1 → P2b → P3 → P5 → submit
```

---

## Git state at handoff

```
Branch: main, commit 64249b7 (clean after merging .praxia/daily.jsonl)

64249b7 merge(p1a): bond_forces alias + test convention fix → main
78d3169 chore: audit record for NPT KE fix
5ac5cfc docs(handoff): p0 parallel close
b23f777 docs: LFMiddle falsification + NPT fix in CLAUDE.md and v1.1_next_steps
b6e5bb9 fix(npt): / mu → * mu; xfail removed
```
