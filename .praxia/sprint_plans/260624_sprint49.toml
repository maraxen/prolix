[sprint]
sprint_id = "260624_sprint49"
sprint_num = 49
name = "Sprint 49: JIT vmap stacked bundles (#2645)"
description = "Track A: stack MolecularBundles + xtrax make_axis_dispatch on N_MOLS"
context = "Sprint 48 #161 done. Homo multi-bundle runs use JIT vmap; hetero stays sequential."
max_fix_retries = 1
worktree_shadow_mode = false

[[tracks]]
id = "a"
is_research = false
worktree = false
skip_worktree = false

[[tracks.items]]
backlog_id = "2645"
phase_name = "Track A — JIT vmap stacked bundles (#2645)"
fixer_prompt = """
Add bundle_stack + ensemble_dispatch; EnsemblePlan._run_stacked_dispatch via
make_axis_dispatch + jax.vmap for homo n_atoms batches. Planner marks N_MOLS
homogeneous when can_jit_vmap_n_mols.
Gate: V3/V4/V5 parity tests pass.
"""
reviewer_prompt = """
VERIFY: uv run pytest tests/api/test_v3_* tests/api/test_v4_* tests/api/test_v5_* tests/api/test_ensemble_vmap_dispatch.py -v
"""
return_key = "2645"
workflow_template = "spec_driven_dev"
