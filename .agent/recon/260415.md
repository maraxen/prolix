# Reconnaissance Report - 2026-04-15

## Explicit Solvent Status
- **Physics**: SPME, PME exclusions, LJ tail corrections, and multiple electrostatic methods (RF, DSF) are implemented and integrated into `system.py`.
- **Validation**: High-parity tests vs OpenMM exist for PME and RF.
- **Solvation**: TIP3P box and solvation pipeline (solvate, merge, pad) are implemented.
- **Constraints**: SETTLE (rigid water) exists in `physics/settle.py` and is used in `batched_simulate.py`, but is **not** integrated into the main `simulate.py` (run_simulation) or `SimulationSpec`.

## Engaging Benchmarks
- **Run History**: Benchmarks were run on 2026-04-13 (`cf9d891`).
- **Location**: Cluster output is structured under `outputs/logs/engaging/YYYYMMDD/`.
- **Local State**: `run_manifest.json` for 2026-04-13 is present, but `app/` and `slurm/` directories are empty locally. This implies the logs were either not pulled or were filtered out by `rsync`.

## Identified Gaps (Undocumented)
1. **SETTLE Integration**: Missing from production `simulate.py` path. Water is flexible by default in the main MD entry point.
2. **SimulationSpec**: No toggle for rigid water / SETTLE.
3. **Implicit GB NL**: Gathering of Born/Energy masks is O(N*K) from O(N^2) matrices, which is a scaling bottleneck for very large systems (though fine for parity).
