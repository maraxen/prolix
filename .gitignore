# Python build artifacts
build/
dist/
*.egg-info/
__pycache__/
*.pyc
*.pyo

# IDE
.idea/
.vscode/
*.swp

# Test/debug output
verify_*.txt
*.log
batch_physics_results.csv

# External dependencies (cloned repos)
scripts/openmmforcefields/
openmmforcefields/
references/kups/

# Generated force field files
# Legacy path from pre-proxide local layout (safe to delete if unused)
src/priox.physics.force_fields/
outputs/
results/

.agent/skills

# Praxia agent infrastructure (ephemeral per-session state)
.praxia/cache/
.praxia/nlm.jsonl
.praxia/tools/
.praxia/workspace.id
*.lock
.env

# Claude Code worktrees (ephemeral per-session)
.claude/worktrees/

# Cluster configuration (local, per-user)
cluster-config.toml

# Test artifacts and reports
tmp/

# HP4 downloaded archives (5.6 GB ANI-1x + COMP6v2 tarballs) — never commit
data/downloads/

# HP4 curated subset (regenerated from archives via fetch_ani1x_subset.py)
data/ani1x_subset/
